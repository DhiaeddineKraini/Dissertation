self.onmessage = event => {
 tification-data',
          resolveDuration,
          // Check if our notification is in notifications
          notificationReturned: notiftno=mc>si s.e a(noin.tag == random)
        });
      });
    })
  );
};