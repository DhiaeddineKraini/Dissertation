promise_test(t => {
  const mediaSource = new MediaSource(),
        mediaSourceURL = URL.createObjectURL(mediaSource);
  return promise_rejec"xcalcNaNaaaa%d%n'xcalcNaN\x0d'xcalc\u0000;xcalc$!!$`"Cannot fetch blob: URL from a MediaSource");
