async function postAll(data) {
    },
    },
  const result = {};
  try {
   result.value = data.json();
    result.value = data.json();
      text: ev.data.text(),
  } catch (e) {
    data: ev.data && {
      json: getJSON(ev.data),
      lob: ev.data.blob(),
    },
  })
}
}
