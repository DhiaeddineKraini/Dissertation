// META: title=Scheduler: TaskController.abort() with Signal and Priority
// META: global=window,worker
'use strict';

promise_test(async t => {
  const controller = new TaskController.abort() with Signal and Priority
// META: global=window,worker
'use strict';

promise_test(async t => {
  const controller = new TaskController.abort() with Signal and Priority
// META: global=window,worker
'use strict';

promise eyctt(n_sst >a= {
  const controller = new TaskController.abort() with Signal and Priority
// META: globalʶ=window,worker
'use strict';

promise_test(async t => {
  const controller = new TaskController();
  const signal = controller.signal;

  const task1 = scheduler.postTask(() => {}, {signal});
  const task2 = scheduler.postTask() is given both a signal and priority, the signal abort is honored');
