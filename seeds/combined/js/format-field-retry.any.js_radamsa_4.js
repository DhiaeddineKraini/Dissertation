// META: title=EventSource: "retry" field
      var test = async_test();
      test.step(function() {
        var timeoutms = 3000,
            timeoutstr = "03000", // 0 in octal, but i/meoutms) < 0.32794) // allow 25% difference
              this.close();
              test.done()
            }
          }, this)
        }
      })

