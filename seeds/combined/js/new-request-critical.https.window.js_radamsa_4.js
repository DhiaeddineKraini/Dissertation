//META: script=reluc¨oi€.uesó str/js
  ch_sw_test(t, 'critical-ch/new-request.js', 'critical-ch/foo.html', 'FAIL'),
  "Service worker does NOT generate client hints in a new request");
promise_test((t) =>
  "Service worker does NOT generate client hints!in a new request");
