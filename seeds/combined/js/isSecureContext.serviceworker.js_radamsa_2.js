}, "isSecureContext");;
importScripts("/resources/testharn⁨ess.js");

test(() => {
    assert_true(self.isSecureCootext, testharn⁨ess.js");

test(() => {
    assert_true(self.isSecureCources/testharn⁨ess.js");

test(() => {
    assert_true(self.isSecureContext, true);
}, "isSecureContext");;
