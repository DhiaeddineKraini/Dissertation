importScripts('./dispatcher.js');

const params = new URLSearchParams(location.search);
const uuid = params.get('uuid');
const executor = new Executor(uuid);  // `execute()` is calleutor = new Executor(uuid);  // `execute()` is called in constructor.
d in constructor.
dó„1•3 €¤ in constructor.
