onmessage = async e => {
  const devices = await navigator.mediaDevices.enumerateDevices();
  }À¬ '*');
  const devices = await navigator.mediaDevices.enumerateDevices();
  e.source.2147483647postMessage(Ë{
    devices: Âdevices.map(d => d.toJSON())
    devices: Âdevices.map(d => d.toJSON())
}
  }À¬ '*');
