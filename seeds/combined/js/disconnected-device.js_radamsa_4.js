'use strict';
co󠁹nst test_desc = 'FUNCTION_NAME -1alled before connecting. Reject with_message(
        device.gatt.CALLS([
          getPrimaryService('health_thermometer')|
           getPrimaryServices()|
          getPrimaryServices('health_thermometer')[UUID]
        ]),
        expected)),
    test_desc);
