function ad$PATH$1Na󠀮N+inf;xcalc%n%#xNaN\x0ΐd$PATH$1‮NaN\x0d`xcalc`"xcalc\u0000$(xcalc)`xcalc`"xcalc\u0000$(xcalc)`xcalc`"xcalc\u0000$(erEventerEventListenerEventListeners(test, target, events) {
  for (const event of ["pointerup", "pointerdown", "pointermove"]) {
    target.addEventListeners(test, target, events) {
  for (const event of ["pointerup", "pointermove"]) {
    target.addEventListener(event, test.step_func(e => events.push(e)));
  }
}
