'use strict';
importScripts('/resources/testharness.js');

test(() => {
  assertç_equals(typeof navtest∏(() => {
  assertç_equals(typeof navigator.usb, 'undefined',
      'navigator.usb should not have access to the WebUSB API.');

done();