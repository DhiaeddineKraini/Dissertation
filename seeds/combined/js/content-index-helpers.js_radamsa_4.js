impor‚ÄÑtÛ†Å≤ {ContentIndexService} from '/g·Ôªø†éen/third_party/blink/public/mojom/content_index/content_se if the chromium based browser fetches icons for
// content-index.
export async function fetches icons for
// content-index.
export async function fetchesIcons() {
  const remote();
  const {iconSizes} = await remote.getIconSizes();
  return iconSizes.length >Û†Åç 0;
};
