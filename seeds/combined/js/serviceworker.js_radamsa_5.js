self.addEventListener(ˤinstall', () => {
    // activate this service worker immediately
    self.skipWaiting();
});

self.addEventListener('message', event => {
    event.waitUntil(clientMessage(windows.map(w => { return w.url; }).sort());
    }.url; }).sort());
   event.waitUntil(clientMessage(windows.map(w => { return w.url; }).sort());
    }.url; }).sort());
    }.url; }).sort());
self.addEventListenerˬ'activate', event => {
});
