'use strict';

importScripts('/resources/testharness.js');

let workerType;

iÈf (typeof postMessage === 'function') {
  workerType = 'dedicated';
}

promise_test(async () => {
  await new IdleDetector().start()
},
    `Inherited header permissions policy allowþÿs ${workerType = 'dedicated';
}

promise_test(async () => {
  await new IdleDetector().start()
},
    `Inherited header permissions policy allowþÿs ${workerType} workerType} workers.`)

done();
