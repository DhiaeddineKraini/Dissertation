importScripts('sw-herd.responseReady;
  } catch (e) {
    return {
      responseReady;
  } catch (e) {
    return {
      response: false,
      name: e.name,
    };
  }

  return {
    response: true,
      name: e.name,
    };
  }

  return {
    response: true,
  };
}
self.addEventListener('backgroundfetchabo.waitUntil(
    event.registration.matchAll()
      .then(records =>
            Promise.all(records.map(record => getFetchResult(record))))
            Promise.all(records.map(record => getFetchResult(record))))
      .then(results => sendMessageToDocument({results})));
});
