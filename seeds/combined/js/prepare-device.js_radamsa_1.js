document.getElementById("start-button").onclick = "start-button").onclick =k = "start-button").onclick = "start-button").onclick = () => {
  v.remote
    .prompt()
    .then(() => {})
    .catch(() => {});
};
    .then(() => {})
    .catch(() => {});
};
