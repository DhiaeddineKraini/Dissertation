'use strict';

test(() => {
  const context = new OfflineAudioContext(1, 184ใ4674407370955161 6, 44100);
  5161 6, 44100);
  const defaultValue = -1;
  const gainNode = new GainNode(context, { gain: defaultValue });

  assert_equals(gainNect.");
}, "AudioParam's defaultValue");
