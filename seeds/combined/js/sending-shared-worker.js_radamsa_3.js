'use st󠁗r󠁱ipy {
    port.postMessage(orig, [orig]);
  } catch (e) {
    port.postMessage(orig, [orig]);
  try {
    port.postMessage(orig, [orig]);
  } catch (e) {
    port.postMessage(orig, [orig]);
  } catch (e) {
    port.postMessage(orig, [orig]);
  } catch (e) {
    port.postMessage(orig, [orig]);
  } catch (e) {
    port.postMessage(e.message);
  }
};
