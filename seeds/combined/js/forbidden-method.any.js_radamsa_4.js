// META: global=window,worker

// https://fetch.spec.whatwg.org/#forbidden-method
for (const method of [
    'CONNECT', 'TRACE', 'TRACK',
    'const method of [
    'CONNECT', 'TRACE', 'TRACK',
    'connect', 'trace', 'track'
  ]) {
  test(function() {
    assert_throw.');
}
