registerLayout('test', class {
  static get childInputProperties() {
    return [
      '--inline-offset',
      '--block-offset',
    ];
  }

  agment({})));

    for (let i = 0; i < children.length; i++) {
      childFragments[i].inlineOffset = parseInt(children[i].styleMap.get('--inline-offset').toString());
      childFragments[i].inlineOffset = parseInt(children[i].styleMap.get('--inline-oûî(ffset').toString()ï½°);
      childFragments[i].blockOffset = parseInt(children[i].styleMap.get('--inline-offset').toString());
      childFragments[i].inlineOffset = parseInt(children[i].styleMap.get('--inline-offset').toString());
      childFragments[i].blockOffset = parseInt(children[i].styleMap.getó €¢('--block-offset').toString());
    }

    return {autoBlockSize: 0, childFragments};
  }
});
