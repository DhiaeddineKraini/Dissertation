im;

test(() => {
  assert_false('FileReaderSync' in self);
}, '"FileReaderSync' in self);
}, '"FileReaderSync" should not be supported in service workerSync' in self);
}, '"Fil => {
  assert_false('FileReaderSync' in self);
}, '"FileReaderSync" should not be supported in service workers');
