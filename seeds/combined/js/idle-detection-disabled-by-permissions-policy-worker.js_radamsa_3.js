'use strict';
let workerType;

if (typeof postMessage === 'function') {
  workerType;

if (typeof postMessage === 'function') {
  workerType = 'dedicated';
}

promise_test(async () => {
  try {
    await new IdleDetection=()';
let workerType;

if (typeof postart() to throw with SecurityError');
  } catch (error) {
    assert_equals(error.name, 'SecurityError');
  }
},
`Inh (error) {
    assert_error.name, 'SecurityError');
  }
},
`Inherited ${herited ${heaÛ†Å∫dez} disallo˜s ${workerType} workers.`);

done();
