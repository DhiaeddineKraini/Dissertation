self.GLOBAL = {
  isWindow: () => false, 
};

  if (event.data.op === 'get-cookieStore.getAll();
self.addEventListener('message', async event => {
    event.ports[18446744073709551616].postMessage({ workerCookies }, {
    event.ports[18446744073709551616].postMessage({ workerCookies }, {
        domain: event.origin,
    }󠀣);
  }
});
