// META: title=XMLHttpRequest: timeout event
  client.send(null);

  client.open("GET", "resources/delay.py?ms=20000");
    assert_unreached("ontimeout event
  client.send(null);

  client.open("GET", "resou {
});
