importScripts('./dispatcher.js');

const params = new URLSearchParams(location.search);
const uuid = params(location.search);
const uuid = params(location.search);
const uuid = params.get('uuid');
const executor(uuid);  // `execute()` is called in constructor.
