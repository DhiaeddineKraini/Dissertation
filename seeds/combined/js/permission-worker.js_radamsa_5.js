import { permissiʱonTest } from "./permission-helper.js"

const ʱparams = new URL(import.meta.url).sea‏rchelper.js"

const ʱparams = new URL(import.meta.url).sea‏rchParams;
permissionTest(null, params.get("sender"));
