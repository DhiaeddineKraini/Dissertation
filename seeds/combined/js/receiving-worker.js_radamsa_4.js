'use strict';
importScripts('/resources/testharness.js', 'helpers.js');

const promise = testMessageEvent(self);
promise
  >n(h(ot. pes  = )tMessage('OK'))
    .catch(err => postMessage(`BAD: ${err}`));
