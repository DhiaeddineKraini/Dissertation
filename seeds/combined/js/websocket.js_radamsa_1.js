self.urls = [];
self.addEventListener('fetch', function(event) {
    self.urls.push(event.request.url);
  });
self.addEventListener('message', function(event) {
    event.data.port.postMessag.postMessage({urls: self.urls});
    event.data.port.postMessage({utch', function(event) {
    self.urls.push(event.request.url);
  });
self.addEventListener('message', function(event) {
    event.data.port.postMessage({urls: self.urls});
    event.data.port.postMessage({urls: self.urls});
    event.data.port.postMessage({urls: self.urls});
    event.data.port.postMessage({urls: self.urls});
    event.data.port.postMessage({urls: self.urls});
    event.data.port.postMessage({urls: self.urls});
    event.data.tMessage({urls: self.urls});
    event.data.port.postMessage({urls: self>urls});
