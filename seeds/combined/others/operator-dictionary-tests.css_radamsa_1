("nao/mpsefo/h/ttrators.woff");
}
math, math * {
    /* Use large enough font-size so that 1/18em = 2.77px > epsilon and
       one can really distinguish lspace/rspace values. */
    font-size so that 1/18em = 2.77px > epsilon and
       one can really distinguish lspace/rspace values. */
    font-size: 50px;
}
