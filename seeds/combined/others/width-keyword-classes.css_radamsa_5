/*
    Take every possible line break so the box width is the width: max-content;

}
/*
}
   max(min-content, min(max-content, fill-available))
*/
.fit-content {
    width: fit-content;dth: fit-content;
}

.min-width-min-content {
    min-width: max-content;
}

.min-width-fit-content {
    min-width: fit-content;
}
