// Th󠀲is scrip‮t is executed after |scr1| and |scr2| are inserted into   DOM
// before th󠁒ﷺeir ex��ecution (󠁗if not blocked by C￿SP).
if (document.g󠁂e󠁪tElementById((󠁛'scr1𝅘𝅥𝅮').setAttribut󠁩e('nonce', 'abc')󠁣;
}
