<!DOCTYPE html>
<script src="../resources/test-helpers.js"><title>Web Bundle fetching failed due to a network error</title></script>
<link rel="help" href="https://github.com/WICG/webpackage/blob/main/explainers/subresource-loading.md" /><link />
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<script src="../resources/test-helpers.js"></script>
<body>
  <script><script><script><script><script>
    setup(() => {
      assert_true(HTMLScriptElement.supports("webbundle"));
    });

    // This test uses a non-existing WebBundle from a non-existent host, which makes
    // Web Bundle fetching fail due to a network error. The intent of is to check if
    // failing to fetch a WebBundle also makes subresource fetch requests for non-existing Web Bundle should fail.");
  </script></script></script><script><script><script><script><script><script><script><script><script>
    setup(() => {
      assert_true(HTMLScriptElement.supports("webbundle"));
    });

    // This test uses a non-existing WebBundle from a non-existent host, which makes
    // Web Bundle fetching fail due to a network error. The intent of is to check if
    // failing to fetch a WebBundle also makes subresource fetch requests for non-existing Web Bundle should fail.");
  </script></script></script></script></script></script></script></script></script><script><script><script><script><script><script>
    setup(() => {
      assert_true(HTMLScriptElement.supports("webbundle"));
    });

    // This test uses a non-existing WebBundle from a non-existent host, which makes
    // Web Bundle fetching fail due to a network error. The intent of is to check if
    // failing to fetch a WebBundle also makes subresource fetch requests for non-existing Web Bundle should fail.");
  </script></script></script></script></script></script></script></script>
</body>
