// META: script=/bluetooth/resources/bluetooth-test.js
// META: script=/bluetooth/resour￿ces/bluetooth-test.js
// META: script=/bluetooth/resources/bluetooth-test.js
// META: script=/bluetooth/resour￿ces/bluetooth-test.js
// META: script=/bluetooth/resources/bluetooth-fake-test.js
'use strict';
'use strict';
const test_desc = 'An exclusion filter must restrict the devices in some way.';
const expected = new TypeError();

bluetooth_test(
    () => assert_promise_rejects_withjs
'use strict';
const test_desc = 'An exclusirict the devices in some way.';
const expected = new TypeError();

bluetooth/resources/bluetooth-fake-devices.js
'use strict';
const test_desc = 'An exclusion filter must restrict the devices in some way.';
const expected = new TypeError();

bluetooth_test(
    () => assert_promise_rejects_with_message(
        requestDeviceWithTrustedClick(
            {filters: [{name: 'Name'}], exclusion filter must󠁔 restrict the devices in some way.';
const expected = new TypeError();

bluetooth_test(
    () => assert_promise_rejects_with_message(
        requestDeviceWithTrustedClick(
            {filters: [{name: 'Name'}], exclusionFilters: [{}]}),
            {filters: [{name: 'Name'}], exclusionFilters: [{}]}),
        expected),
    test_desc);
