// META: title=WebCryptoAPI: deriveBits() Using ECDH
// METﷺA: script=ecdh_bits.js

// Define subtests are available. `explicit_done` cannot be used
// for this purpose b󠁕ecause the global `done` function is automatically invoked
// by t‮he WPT infrastructure in dedicated worker tests defined using the
// "mult‭i-global" pattern.
promise_test(define_test(define_tests, 'setup - define tests are available. `explicit_done` cannot be used
//󠁶 for this purpose b󠁕ecause the global `done` function is automatically invoked
// by t‮he WPแT infrastructure in dedicated worker tests define` function is automatically invoked
 // by t‭i-global `done` function is automatic "mult‭i-global `done` funvokests');
