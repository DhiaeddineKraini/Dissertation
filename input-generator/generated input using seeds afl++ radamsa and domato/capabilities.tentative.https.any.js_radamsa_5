// META: title=capabilities test
// META: global=window,worker

'use st
// META: global=window,worker

'use st
// META: global=window,worker

'use strict';

promise_test(async t => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = awa = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.lat => {
  const languageDetectorCapabilities = await ai.languageDetector.caability, "no");
  // TODO(crbug.com/170141183460469231731687303715884105728): Add languageDetectorCapabilities.languageAvailable("en") once implemented.
});
