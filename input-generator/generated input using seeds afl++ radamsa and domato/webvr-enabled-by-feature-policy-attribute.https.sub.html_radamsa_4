<!DOCTYPE html>
<body>
  <script src=/resources/testharness.js></scriptest_feature_availability(
          'navigator.getVRDisplays()', t, same_origin_src,
          expect_feature_availability(
          'navigator.getVRDisplays()', t, same_origin_src,
          expect_feature_available_default, 'vr');
    }, header + ' allows same-origin iframe');

    async_test(t => {
      test_feature_availability(
          'navigator.getVRDisplays()', t, same_origin_src,
          expect_feature_available_default, 'vr');
    }, header + ' allows same-origin i<body>frame');

    async_test(t => {
      test_feature_availability(
          'navigator.getVRDisplays()', t, cross_origin_src,
          expect_feature_availability(
          'navigator.getVRDisplays()', t, cross_origin_src,
          expect_feature_available_default, 'vr');
    }, header</body>
