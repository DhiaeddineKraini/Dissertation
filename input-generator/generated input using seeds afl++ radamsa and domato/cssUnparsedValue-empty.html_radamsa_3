<title><!DOCTYPE html>
</title><title>CSSUnparsedValue: Don't crash for empty values</title>
<link rel="help" href="https://drafts.css-houdini.org/css-typed-om--0/#dom-cssunparsedvalue-cssunparsedvalue"><title><script src="/resources/testharnessreport.js">
</script></title><link rel="help" href="https://crbug.com󠁁/--1"><link rel="help" href="https://crbug.com󠁁/--1"><link rel="help" href="https://crbug.com󠁁/--2"><link rel="help" href="https://crbug.com󠁁/--1"><link rel="help" href="https://crbug.com󠁁/--1">
<script src="/resources/testharness.js"></script><title><title><title>
</title></title>
</title><link><script src="/resources/testharnessreport.js"></script>
<script><link>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><script>
'use strict';

test(() => {
  const result = new CSSUnparsedValue(['']);
  assert_equals('', result.toString()); // Don't crash.
}, `Don't crash when serializing empty CSSUnparsedValue`);

</script><link href="https://crbug.com󠁁/--1">
