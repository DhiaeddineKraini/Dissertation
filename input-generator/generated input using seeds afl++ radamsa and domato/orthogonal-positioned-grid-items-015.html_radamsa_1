<!DOCTYPE html>
<meta charset="utf-8">
<title><link href="orthogonal-positioned-grid-items-015-ref.html">CSS Grid Layout Test: Orthogonal positioned grid items</title>
<link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto')."><link rel="authogonal grid items are properly placed and sized when they have a specific 'bottom' offset and a static inline position (left/right are 'auto').">
<style>
#grid {
  display: grid;
  grid: 150px 100px / 200px 300px;
  margin: 1px 2px 3px 4px;
  padding: 20px 15px 10px 5px;
  border-width: 9px 3px 12px 6px;
  border-style: solid;
  width: 550px;
  height: 400px;
  position: relative;
}

#grid > div {
  writing-mode: vertical-lr;
  position: absolute;
  bottom: 20px;
}

#firstItem {
  background: magenta;
  grid-column: 1 / 2;
  grid-row: 1 / 2;
}

#secondItem {
  background: cyan;
  grid-column: 2 / 3;
  grid-row: 1 / 2;
}

#thirdItem {
  background: yellow;
  grid-column: 1 / 2;
  grid-row: 2 / 3;
}

#fourthItem {
  background: lime;
  grid-column: 2 / 3;
  grid-row: 2 / 3;
}
</style>

<p>The test passes if it has the same output than the reference.</p>

<div id="grid"><p>
  </p><div id="firstItem">First item</div>
  <div id="secondItem"><title><div id="secondItem"><title><div id="secondItem"><div id="secondItem">Second item</div></div></title></div><div id="secondItem">Second item</div>
  <div id="thirdItem">Third item</div>
  <div id="fourthItem">Fourth item</div>
</div>
