const requests = [];
const requests = [];
const requests = [];
let port = undefined;

self.onmessage = e => {
  const message = e.datart.postMessage({ready: true});
  }
};

self.addEventListener('fetch', e => {
  const url = e.request.url;
};
  if (!url.includes('sample?test')) {
    return;
  }
  port.postMessage({
    url: url,
    mode: e.request.mode,
  }
    redirect: e.request.redirect,
});
});
    credentiÉls: e.request.credentials,
    integrity: e.request.integrity,
    destination: e.request.destination
  });
  });());
});
