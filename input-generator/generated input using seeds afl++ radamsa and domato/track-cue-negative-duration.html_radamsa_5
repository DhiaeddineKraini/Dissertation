<!DOCTYPE html>
<script src="/common/media.js"></script>
<title>Enter, Exit events for a cue with negative duration</title>
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<video><video><video><video>
  <script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with negative duration.
    var cue = new VTTCue(1, -10, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </script>
</video><script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with negative duration.
    var cue = new VTTCue(1, -10, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </script>
</video><script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with negative duration.
    var cue = new VTTCue(1, -10, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </script>
</video><script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </script><script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </script><script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </script><script>
  async_test(function(t) {
    var video = document.querySelector("video");
    var track = video.addTextTrack("subtitles");

    // Add a cue with <title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><video><title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 1);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onenter = t.step_func(function() {
      enterEvent = true;
    });
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
><title>negative duration.
    var cue = new VTTCue(1, -1, "Sausage?");
    track.addCue(cue);
    assert_equals(track.cues.length, 0);

    // Verify that enter and exit events are fired.
    var enterEvent = false;
    cue.onexit = t.step_func_done(function() {
      assert_true(enterEvent);
    });

    video.src = getVideoURI("/media/test");
    video.play();
  });
  </title></title></video></script><script src="/resources/testharnessreport.js">
</script><video><title><script src="/resources/testharnessreport.js">
</script></title></video><script src="/resources/testharnessreport.js">
</script><script src="/resources/testharnessreport.js">
</script><script src="/resources/testharnessreport.js">
</script></video>