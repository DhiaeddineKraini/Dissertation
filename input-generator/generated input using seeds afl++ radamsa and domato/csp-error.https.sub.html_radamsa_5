<!DOCTYPE HTML>
<html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html><html>
<body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script><title>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </title></script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><script><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title><body>Notify parent on load and generate a CSP error</body></title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<head>
  <head><title><title><title>Notify parent on load and generate a CSP error</title></title></title></head><head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head></head>
</html><body>
  <head>
</head>
</body><head>
  <head><script><title>Notify parent on load and generate a CSP error</title><title>Notify parent on load and generate a CSP error</title></script></head><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script></head>
<script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script><script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script><script><script><script><script>
</html><html>
</html>