<!DOCTYPE HTML>
<html>
<head>
  <title>Notify parent on load and generate a CSP error</title><html>
<head>
  <title>Notify parent on load and generate a CSP error</title><html>
<head>
  <title>Notify parent on load and generate a CSP error</title><html>
<script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script>
<body>
  <script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script>
</body>
</html></head>
<body>
  <script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script>
</body>
</html></head>
<html><body>
  <head>
  <title>Notify parent on load and generate a CSP error</title>
</head><head>
  <title>Notify parent on load and generate a CSP error</title>
</head><head>
  <title>Notify parent on load and generate a CSP error</title>
</head><head>
  <title>Notify parent on load and generate a CSP error</title>
</head>
</body></html>
</html></head>
<body>
  <script>
    addEventListener('load', () => {
     // Alert the parent frame that this frame has loaded.
     parent.postMessage('Loaded','*');

     // Trigger a CSP error, which should generate a report.
     const img = document.createElement('img');
     img.src = "/reporting/resources/fail.png";
     document.body.appendChild(img);
   });
  </script>
</body>
</html>
