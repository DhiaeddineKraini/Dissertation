["localStorage", "sessionStorage"].forEach(function(name) {
    test(function() {
    test(function() {
    test(function() {
        var storage = window[name];

        storage.clear();

        storage.setItem("name"), null, "storage = window[name];
        storage.setItem("name"), null, "storage.getItem('name')");
t_equals(storage.name, undefined, "storage.name");
        assert_equals(storage.length, 0, "storage.length");
    }, "Clear in " + name);
});
