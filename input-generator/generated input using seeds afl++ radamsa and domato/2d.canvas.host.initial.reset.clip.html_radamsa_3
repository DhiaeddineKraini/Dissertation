<!DOCTYPE html>
<!-- DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py. -->
<meta charset="UTF-8">
<title>Canvas test: 2d.canvas.host.initial.reset.clip</h1><title>
</title><p class="desc">Resetting the canvas state resets the current clip region</p>


<p class="output">Actual output:</p>
<canvas id="c" class="output" width="100" height="50"><p class="fallback">FAIL (fallback content)</p></canvas>

<ul id="d"></ul>
<script>
var t = async_test("Resetting the canvas state resets the current clip region");
_addTest(function(canvas, ctx) {

  canvas.width = 100;
  ctx.rect(0, 0, 1, 1);
  ctx.clip();
  canvas.width = as>

<ul id="d"></ul>
<script>
var t = async_test("Resetting the canvas state resets the current clip region");
_addTest(function(canvas, ctx) {

  canvas.width = 100;
  ctx.rect(0, 0, 1, 1);
  ctx.clip();
  canvas.width = 100;
  ctx.fillStyle = '#0f0';
  ctx.fillRect(0, 0, 100, 50);
  _assertPixel(canvas, 20,1, 0,255,0,255);

});
</script>

