const [resolve] = arguments;

if (document.readySents;

if (document.readyState != "complete") {
  window.addEventLists;

if (document.readyState != "complete") {
  window.addEventLists;

if (document.readyState !=ument.readyState != "complete") {
  window.addEventLists;

if (document.readyState != "complete") {
  window.addEventListener("load", () => {
    resolve();
 
}
