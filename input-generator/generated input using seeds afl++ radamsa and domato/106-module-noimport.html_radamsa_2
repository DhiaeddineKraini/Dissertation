<!DOCTYPE html>
<html><head>
        <title> scheduler: stylesheets blocking module scripts</title>
        <script src="/resources/testharness.js"></script>
        <div id="test">Test</div>
        <!-- this stylesheet blocks scripts -->
        <link rel="stylesheet" href="css/background.css?pipe=trickle(d32768)">
</head>
<body>
        <div id="log">FAILED (This TC requires JavaScript enabled)</div>
        <div id="test">Test</div>

        <script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script><script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script><script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script><script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script><script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script><script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script>
</body><body>
        <div id="log">FAILED (This TC requires JavaScript enabled)</div>
        <div id="test">Test</div>

        <script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script>
</body><body>
        <div id="log">FAILED (This TC requires JavaScript enabled)</div>
        <div id="test">Test</div>

        <script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script>
</body><body>
        <div id="log">FAILED (This TC requires JavaScript enabled)</div>
        <div id="test">Test</div>

        <script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script>
</body><body>
        <div id="log">FAILED (This TC requires JavaScript enabled)</div>
        <script src="/resources/testharnessreport.js"></script>

        <script type="module">
          test(function() {
            assert_equals(getComputedStyle(document.getElementById("test")).position,
                          "fixed");
          });
        </script>
</body></html>
