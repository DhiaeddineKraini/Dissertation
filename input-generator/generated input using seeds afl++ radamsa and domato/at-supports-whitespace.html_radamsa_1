<title><!DOCTYPE html>
</title><title><!DOCTYPE html>
</title><title><!DOCTYPE html>
</title><title><!DOCTYPE html>
</title><script><title><!DOCTYPE html>
</title><title><!DOCTYPE html>
</title><title>Parsing of @supports with whitespace</title>
<link rel="help" href="https://drafts.csswg.org/css-conditional-3/#at-supports">
<script src=/resources/testharness.js></script>
    '@supports ( (a) or (b)) {}',
  }

      t.add_cleanup(() => style.remove());
  let examples = [
    '@supports ((a) or (b)) {}',
    '@supports ( (a) and (b) ) {}',
    '@supports ( (a) or (b) ) {}',

      assert_equals(style.sheet.rules.length, 1);

    '@supports ( (a) ) {}',
<script src=/resources/testharnessreport.js></script>
    '@supports (not (a) ) {}',
  ];
    '@supports ( (a) and (b)) {}',
    '@supports ((a)) {}',
    '@supports ((a) and (b)) {}',

  let examples = [
    '@supports (not (a)) {}',
    '@supports (not (a) ) {}',
    '@supports ((a)) {}',
      style.textContent = example;
    test((t) => {
    '@supports ( (a)) {}',
    '@supports ( (a) ) {}',
<main id=main></main><script>
  for (let example of examples) {

    '@supports ((a) ) {}',
<script><script><script><script><script><script><script><script><title><!DOCTYPE html>
</title><script><script><script><script><script><script><main><script></script></main></script><script><script><script><script>
    '@supports ((a) and (b) ) {}',
    '@supports ((a) or (b) ) {}',
</script></script><script><script>
    '@supports ((a) and (b) ) {}',
    '@supports ((a) or (b) ) {}',
</script></script></script></script><script><script>
    '@supports ( not (a) ) {}',
    }, example);
    '@supports (not (a)) {}',
    '@supports ( (a)) {}',
      main.append(style);
    '@supports ( not (a)) {}',
      let style = document.createElement('style');
    '@supports ((a) ) {}',
    '@supports ( not (a)) {}',
    '@supports ( not (a) ) {}',

    '@supports ((a) and (b)) {}',
    '@supports ((a) and (b) ) {}',
    '@supports ( (a) and (b)) {}',
    '@supports ( (a) and (b) ) {}',

    '@supports ((a) or (b)) {}',
    '@supports ((a) or (b) ) {}',
    '@supports ( (a) or (b)) {}',
    '@supports ( (a) or (b) ) {}',
  ];

  for (let example of examples) {
    test((t) => {
      let style = document.createElement('style');
      t.add_cleanup(() => style.remove());
      style.textContent = example;
      main.append(style);
      assert_equals(style.sheet.rules.length, 1);
    }, example);
  }
</script></script></script><script><script><script>
  let examples = [
    '@supports ((a)) {}',
    '@supports ((a) ) {}',
    '@supports ( (a)) {}',
    '@supports ( (a) ) {}',

    '@supports (not (a)) {}',
    '@supports (not (a) ) {}',
    '@supports ( not (a)) {}',
    '@supports ( not (a) ) {}',

    '@supports ((a) and (b)) {}',
    '@supports ((a) and (b) ) {}',
    '@supports ( (a) and (b)) {}',
    '@supports ( (a) and (b) ) {}',

    '@supports ((a) or (b)) {}',
    '@supports ((a) or (b) ) {}',
    '@supports ( (a) or (b)) {}',
    '@supports ( (a) or (b) ) {}',
  ];

  for (let example of examples) {
    test((t) => {
      let style = document.createElement('style');
      t.add_cleanup(() => style.remove());
      style.textContent = example;
      main.append(style);
      assert_equals(style.sheet.rules.length, 1);
    }, example);
  }
</script></script></script><script><script><script>
  let examples = [
    '@supports ((a)) {}',
    '@supports ((a) ) {}',
    '@supports ( (a)) {}',
    '@supports ( (a) ) {}',

    '@supports (not (a)) {}',
    '@supports (not (a) ) {}',
    '@supports ( not (a)) {}',
    '@supports ( not (a) ) {}',

    '@supports ((a) and (b)) {}',
    '@supports ((a) and (b) ) {}',
    '@supports ( (a) and (b)) {}',
    '@supports ( (a) and (b) ) {}',

    '@supports ((a) or (b)) {}',
    '@supports ((a) or (b) ) {}',
    '@supports ( (a) or (b)) {}',
    '@supports ( (a) or (b) ) {}',
  ];

  for (let example of examples) {
    test((t) => {
      let style = document.createElement('style');
      t.add_cleanup(() => style.remove());
      style.textContent = example;
      main.append(style);
      assert_equals(style.sheet.rules.length, 1);
    }, example);
  }
</script></script></script></script></script></script></script><script><script><script><script>
  let examples = [
    '@supports ((a)) {}',
    '@supports ((a) ) {}',
    '@supports ( (a)) {}',
    '@supports ( (a) ) {}',

    '@supports (not (a)) {}',
    '@supports (not (a) ) {}',
    '@supports ( not (a)) {}',
    '@supports ( not (a) ) {}',

    '@supports ((a) and (b)) {}',
    '@supports ((a) and (b) ) {}',
    '@supports ( (a) and (b)) {}',
    '@supports ( (a) and (b) ) {}',

    '@supports ((a) or (b)) {}',
    '@supports ((a) or (b) ) {}',
    '@supports ( (a) or (b)) {}',
    '@supports ( (a) or (b) ) {}',
  ];

  for (let example of examples) {
    test((t) => {
      let style = document.createElement('style');
      t.add_cleanup(() => style.remove());
      style.textContent = example;
      main.append(style);
      assert_equals(style.sheet.rules.length, 1);
    }, example);
  }
</script></script></script></script></script></script></script></script></script></script></script></script></script>
