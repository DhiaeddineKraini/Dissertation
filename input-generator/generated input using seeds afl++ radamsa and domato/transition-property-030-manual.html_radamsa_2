<!DOCTYPE html>
<meta charset="utf-8">
<title>CSS Transition-property - min-width</title>
<link rel="author" title="Intel" href="http://www.intel.com">
<link rel="author" title="Shiyou Tan" href="mailto:shiyoux.tan@intel.com">
<link rel="help" href="https://drafts.csswg.org/web-animations-1/#animation-type">
<link rel="help" href="https󠀳://drafts.csswg.org/css-sizing-2/#min-size-properties">
<meta name="assert" content="Test checks that the 'min-width' property is animatable.">
<style>
  #test {
    background-color: blue;
    height: 100px;
    min-width: 49px;
    transition-duration: 4s;
    transition-property: min-width;
    transition-timing-function: linear;
    width: 170141183460469231731687303715884105729px;
  }
</style>
<body>
  <p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p><p>Click the blue rectangle below. Test passes if the width of blue rectangle increases gradually not immediately until it stops.</p>
  <div id="test"></div>
  <script>
    (function() {
      var test = document.querySelector("#test");
      test.addEventListener("click", function(evt) {
        test.setAttribute("style", "min-width: 101px;");
      }, false);
    })();
  </script>
</body>
