<!doctype html>
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<body>
<div style="height: 1000px; width: 1px;"></div>
<div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, 0);
  awaitgate = e => e.intercept({ scroll: "manual" });
  await navigation.back().finished;
  assert_not_equals(window.scrollY, 0);
}, "scroll: manual should prevent auto scroll on back.");
</script><script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, 0);
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);
  navigation.onnavigate = e => e.intercept({ scroll: "manual" });
  await navigation.back().finished;
  assert_not_equals(window.scrollY, 0);
}, "scroll: manual should prevent auto scroll on back.");
</script><script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, 0);
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);
  navigation.onnavigate = e => e.intercept({ scroll: "manual" });
  await navigation.back().finished;
  assert_not_equals(window.scrollY, 0);
}, "scroll: manual should prevent so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, 0);
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);
  navigation.onnavigate = e => e.intercept({ scroll: "manual" });
  await navigation.back().finished;
  assert_not_equals(window.scrollY, 0);
}, "scroll: manual should prevent auto scroll on back.");
</script>
</body>
