<!DOCTYPE html>
<script>

window.addEventListener('message', (e) => {
  const buffer = e.data;
  e.source.postMessage('started', '*');

</script><script>

window.addEventListener('message', (e) => {
  const buffer = e.data;
  e.source.postMessage('started', '*');

</script><script>

window.addEventListener('message', (e) => {
  const buffer = e.data;
  e.source.postMessage('started', '*');

</script><script>

window.addEventListener('message', (e) => {
  const buffer = e.data;
  e.source.postMessage('started', '*');

</script>
