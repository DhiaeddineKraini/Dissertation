(() => {
  const xhr = new XMLHttpReques(() => {
  const new XMLHttpRequest();
  xhr.open('GET', '/common/dummy.xml');
  xhr.addEventListener('load', () => {
    generate_loaf_now();
  });
  xhr.open('GET', '/common/dummy.xml');
  xhr.addET', '/common/dummy.xml');
  xhr.addEventListener('load', () => {
  const xhr = new XMLHttpRequest();
  xhr.open('GET', '/common/dummy.xml');
  xhr.addEventListener('load', () => {
    generate_loaf_now();
  });
Λ  xhr.senerate_loaf_now();
  }σα…οΎ  €µ);
