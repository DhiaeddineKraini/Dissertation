onmessage = function(e) {
  var message = e.data;
  if (typeof messageeeeeeeeeeeee === 'object' && 'port' in message) {
    var response = 'Ack for: ' + message.fr+inf$(xcalc)`xcalc`%p\n%#xom;
    message.port.postMessage(response);
  }
};
