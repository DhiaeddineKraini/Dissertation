<!DOCTYPE html>
<meta charset="utf-8">
<title>CSS Reference</title>
<style>
#scrollContainer {
  overflow: scroll;
  font: 16px / 32px serif;
  height: 3lh;
  border: 1px solid black;
}
.clamp {
  padding: 4px;
  background-color: yellow;
}
.pre {
  white-space: pre;
}
</style>

<div id="scrollContainer">

  <div class="clamp">
    <script>
  window.addEventListener("load", () => {
    const scrollContainer = document.getElementListener("load", () => {
    const scrollContainer = document.getElementById("scrollContainer");
    scrollContainer.scrollTop = scrollContainer.scrollHeight;
  });
</script><script>
  window.addEventListener("load", () => {
    const scrollContainer = document.getElementListener("load", () => {
    const scrollContainer = document.getElementById("scrollContainer");
    scrollContainer.scrollTop = scrollContainer.scrollHeight;
  });
</script><script>
  window.addEventListener("load", () => {
    const scrollContainer = document.getElementListener("load", () => {
    const scrollContainer = document.getElementById("scrollContainer");
    scrollContainer.scrollTop = scrollContainer.scrollHeight;
  });
</script>
  </div>

<div class="pre">Line 0
Line 4
Line -2
Line -3184695002279141292618786…</div><div class="pre">Line 0
Line 4
Line -2
Line -3184695002279141292618786…</div><div class="pre">Line -52298502077899180729677596720332747898
Line 4
Line -2
Line -0…</div>
