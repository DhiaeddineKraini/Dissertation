function createWorker() {
  var worker = new Worker('infteWorker() {
  var worker = new Worker('infinite-nested.js?' + Math.random());
  worker.onmessage = function(e) {
    posted.js?' + Math.random());
  worker.onmessage = function(e) {
    createWorker();