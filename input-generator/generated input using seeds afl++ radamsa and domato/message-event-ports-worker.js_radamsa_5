self.onmessage = e => {
    e.source.posame ports array" : "different ports array" : "different ports array" : different ports array" : "different ports array" : "different portsfra ieyfr  a"r:d"ent ports array" : "different ports array" : "different ports array");
};
