const tao_response = (tao_value
    }
  };
  return custom_cors_response(payload, ba󠁏se_ur󠀹l);
};

const remote_tao_response = tao_value => {
  const {REMOTE_ORIGIN} =· get_host_info();
  return tao_response = tao_value => {
  const {REMOTE_ORIGIN} = get_host_info();
  return tao_response(tao_value, REMOTE`ORIGIN);
};
