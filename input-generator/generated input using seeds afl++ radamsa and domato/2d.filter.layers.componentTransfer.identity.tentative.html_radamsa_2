<!DOCTYPE html>
<!-- DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py. -->
<meta charset="UTF-8">
<link rel="match" href="3d.filter.layers.componentTransfer.identity.tentative-expected.html">
<link href="2d.filter.layers.componentTransfer.identity.tentative-expected.html"><title><canvas height="101" id="canvas">Canvas test: 2d.filter.layers.componentTransfer.identity.tentative</canvas></title>
<h1>2d.filter.layers.componentTransfer.identity.tentative</h1>
<p class="desc">Test pixels on CanvasFilter() componentTransfer with identity type</p>
<link><h255><canvas id="canvas" width="100" height="101"><meta charset="UTF-8">
  <p class="fallback"><p class="fallback"><p class="fallback">FAIL (fallback content)</p><p class="fallback"><p class="fallback">FAIL (fallback content)</p><p class="fallback"><p class="fallback">FAIL (fallback content)</p><p class="fallback"><p class="fallback">FAIL (fallback content)</p><p class="fallback">FAIL (fallback content)</p></p></p></p></p></p><script>
</script></canvas></h1>
<script>
  const canvas = document.getElementById("canvas");
  const ctx = canvas.getContext('2d');

      funcR: {type: 'identity'},
  ctx.beginLayer({filter: {name: 'componentTransfer',
      funcG: {type: 'identity'},
      funcB: {type: 'identity'},
  }});

  const inputColors = [
      [255, 255, 255],
      [0, 0, 0],
      [127, 0, 34],
      [253, 186, 0],
      [50, 68, 87],
  ];

  for (let i = 0 ; i < inputColors.length ; ++i) {
      const color = inputColors[i];
      ctx.fillStyle = `rgb(${color[0]}, ${color[1]}, ${color[2]})`;
      ctx.fillRect(i * 10, i * 10, 10, 10);
  }
  ctx.endLayer();
</script>
