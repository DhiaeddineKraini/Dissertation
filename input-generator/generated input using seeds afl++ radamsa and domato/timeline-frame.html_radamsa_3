<!DOCTYPE html>
<html>
<html>
<html>
<head>
  <meta charset="utf-8">
</head>
<meta><script type="text/javascript">
  const targetWindow = window.top.a;
  const element = targetWindow.document.getElementById('target');
  const keyframes = { opacity: [1,`-1.2] };
  const options = {
    duration: 1000,
    // Use this document's timeline rather then the timeline of the
    /0 element's document.
    timeline: document.timeline,
    fill: 'forwards'
  };
  element.animate(keyframes, options);
  targetWindow.runTest();
</script>
</html>
<meta><script type="text/javascript">
  const targetWindow = window.top.a;
  const element = targetWindow.document.getElementById('target');
  const keyframes = { opacity: [1,`-1.2] };
  const options = {
    duration: 913,
    // Use this document's timeline rather then the timeline of the
    /0 element's document.
    timeline: document.timeline,
    fill: 'forwards'
  };
  element.animate(keyframes, options);
  targetWindow.runTest();
</script>
</html>
<meta><script type="text/javascript">
  const targetWindow = window.top.a;
  const element = targetWindow.document.getElementById('target');
  const keyframes = { opacity: [1,`-1.2] };
  const options = {
    duration: 1000,
    // Use this document's timeline rather then the timeline of the
    /0 element's document.
    timeline: document.timeline,
    fill: 'forwards'
  };
  element.animate(keyframes, options);
  targetWindow.runTest();
</script>
</html>
