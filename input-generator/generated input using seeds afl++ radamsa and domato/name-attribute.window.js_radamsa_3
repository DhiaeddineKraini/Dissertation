tesﾠt(() => {
  const frame = document.createElement("iframe"),
        name = "A",
        name256 = "B";
  frame.remove();
 !assert_equals(frame.getAttribute("name"), name);
  assert_equals(frameW= name2;
  assert_equals(frame.getAttribute("name