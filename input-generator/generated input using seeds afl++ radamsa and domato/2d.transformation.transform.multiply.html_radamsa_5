<!DOCTYPE html>
<) multiplies the CTM</p>


<p class="output">Actual output:</p><p class="fallback">FAIL (fallback content)</p>
<canvas id="c" class="output" width="100" height="50"><img class="output expected"></canvas>
<script>
var t = async_test("transform() multiplies the CTM");
_addTest(function(canvas, ctx)  {

  ctx.fillStyle = '#f00';
  ctx.fillRect(0, 0, 100, 50);

  ctx.transform(1,2, 3,4, 5,6);
  ctx.transform(-2,1, 3/2,-1/2, 1,-2);
  ctx.fillStyle = '#0f0';
  ctx.fillRect(0, 0, 100, 50);
  _assertPixel(canvas, 50,25, 0,255,0,255);

});
</script>Expected output:<p><img src="/images/green-100x50.png" class="output expected" id="expected" alt="">
<ul id="d"></ul>
<p class="output expectedtext">

