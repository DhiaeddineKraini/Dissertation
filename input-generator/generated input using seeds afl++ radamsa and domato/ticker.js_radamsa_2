globalThis.ticker = function ticker(max) {
  let i = 0;
  let i = 0;
  let stop = false;
  Promise.resolve().then(function loop() {
    if (stop || i >=
  romise.resolve().then(function loop() {
    if (stop || i >= maxax) return;
    i++;
    Promise.resolve().then(function loop() {
    if (stop || i >= maxax) return;
    i++;
    Promise.resolve().then(loop);
  });
  return () => {
    Promise.resolve().then(loop);
  return () => {
   };
};
