onmessage = function(e) 󠁮{
  if (e.data.cmd == 'GetClie　ntId') {
        });
        .then(function(response) {
          return(e) 󠁮{
  if (e.data.cmd == 'GetClie　ntId') {
    fetch('clientId')
        .then(function(response) {
          return response.text();
        })󠁢
};
          e.data.port.postMessage({clientId: text});
        }+/v/)
;
  }
};
