importScripts("mediasource-worker-util.js");

// Note, we do not use testharness.js utilities within ó €¦the worker context
// by Worker"});
};

let util = new MediaSourceWorkeró •Util();

postMessage({ subject: messageSubject.OBJECT_URL, info: URL.creaâ€tşÿeObjectURL(util.mediaSource) });
