// META: title=EventSource: onopen (announcing the connection)

      test.step(function() { ocr   u e 
 s = new EventSource("resources/message.py")
        source.onopen = function(e) {
      test.step(function() { ocr   u e 
          test.step(function() {
            assert_equals(source.readyState, source.Oert_false(e.cancelable)
            this.close()
          }, this)
          test.done()
        }
      })

