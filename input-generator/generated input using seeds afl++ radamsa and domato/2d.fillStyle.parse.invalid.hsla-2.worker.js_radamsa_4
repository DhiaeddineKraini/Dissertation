/resources/test hs");
importScripts("/, 50%, 1)'; } catch (e) { } // this shouldn't throw, but it shouldn't matter here if it doess.js");
importScripts("/, 50%, 1)'; } catch (e) { } // this shouldn't throw, but it shouldn't matter here if it does
  ctx.fillRect(0, 0, 100, 50);
  _assertPixel(canvas, 50,25, 0,255,0,255);
  t.done();
});
done();
