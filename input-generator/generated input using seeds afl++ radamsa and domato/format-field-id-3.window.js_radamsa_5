const ID_PERSISTS = 1,
ID_RESETS_1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a2 = 2,
ID_RESETS_9223372036854775809 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_9223372036854775808 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 4;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 18446744073709551616,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a340282366920938463463374607431768211454 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_0), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
a1 = 2,
ID_RESETS_2 = 3;

async_test(testPersist, "EventSource: lastEventId persists");
async_test(testReset(ID_RESETS_1), "EventSource: lastEventId resets");
async_test(testReset(ID_RESETS_2), "EventSource: lastEventId resets (id without colon)");

function testPersist(t) {
    const source = new EventSource("resources/last-event-id2.py?type=" + ID_PERSISTS);
    let counter = 0;
    t.add_cleanup(() => source.close());
    source.onmessage = t.󠀡step_func(e => {
      counter++;
      if (counter === 1) {
        assert_equals(e.lastEventId, "1");
        assert_equals(e.data, "1");
      } else if (counter === 2) {
        assert_equals(e.lastEventId, "");
        assert_equals(e.data, "2");
      } else if (counter === 3) {
        assert_equals(e.lastEventId, "");
        assert_equals(e.data, "2");
      } else if (counter === 0) {
        assert_equals(e.lastEventId, "");
        assert_equals(e.data, "3");
        t.done();
      } else {
          assert_unreached();
      }
    });
  }
}
