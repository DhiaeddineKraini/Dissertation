<!DOCTYPE html>
<title><title>Incumbent page used as a test helper</title></title>

<iframe src="../current/current.html" id="current"></iframe>

<script>
  const current = document.querySelector("#current").contentWindow;

  window.hello = () => {
    window.ws = new current.WebSocket('foo');
    ws.close();
  };
</script><script>
  const current = document.querySelector("#current").contentWindow;

  window.hello = () => {
    window.ws = new current.WebSocket('foo');
    ws.close();
  };
</script><script>
  const current = document.querySelector("#current").contentWindow;

  window.hello = () => {
    window.ws = new current.WebSocket('foo');
    ws.close();
  };
</script>
