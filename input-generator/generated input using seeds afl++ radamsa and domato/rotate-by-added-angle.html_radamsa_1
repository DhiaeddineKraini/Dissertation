<!DOCTYPE html>
<link rel="author" title="Xiaocheng H 100px;
  position: absolute;
  top: 100px;
  left: 100px;
}
.ref {
  transform: rotate(90deg);
  background-color: red;
}
.test {
  background-color: green;
  z-index: 1;
}
</style>
<script>
const angle = new CSSMathSum(CSS.deg(45), CSS.turn(0.125)); // 90 degrees
const transform = new CSSTransformValue([new CSSRotate(angle)]);
const target = document.querySelector('.test');
target.attributeStyleMap.set('transform', transform);
</script>
<div class="common ref"></div>
<div class="common test"></div>
<script><script><script><p>Test passes if there is a filled green rectangle with <strong>no red</strong>.</p></script></script></script>
