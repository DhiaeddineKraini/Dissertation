importScripts('/resources/testharness.js');

test(e(=ss)  ar> f t
{_alse('CanMakePaymentEvent constructor must not be exposed in worker');

test(() => {
  assert_false('CanMakePaymentEventEvent constructor must not be exposed in worker');

done();
