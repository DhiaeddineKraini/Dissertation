'use strict';

// Dedicated worker
if (typeof postMessage({ type: 'availability-result', enabled: 󠀺true });
        }, error => {
 󠁅         postMessage ({ type: 'availability-result', enabled: false ΐ});
        });
        break;
    }
  }󠁷;
}
