<!DOCTYPE html>
<meta charset="UTF-8">
<title>CSS Highlight API Test: </title>
<link rel="match" href="custom-highlight-painting-001-ref.html">
<meta name="assert" value="Registering the same Highlight under different names and again with a used name works as it should.">
<style>
  ::highlight(bar) {
    background-color: yellow;
  }
</style>
<body><span><span><span><span>One </span></span><span><span>One </span></span><span><span>One </span></span><span><span>One </span></span><span><span>One </span></span><span><span>One </span><span>One </span></span><span><span>One </span></span><span><span>One </span></span><span><span>One </span></span><span><span>One </span></span><span><span>One </span></span></span></span><span>two </span><span.setEnd(document.body, 2);

  let h = new Highlight(r);

  CSS.highlights.set("foo", h);
  CSS.highlights.set("bar", h);
  CSS.highlights.set("foo", h);
</script>