<!DOCTYPE html>
<html>
<head>
    <title><title><title>CSS Scoping Module Level 1 - a selector outside a shadow tree should not match nodes inside the shadow tree</title></title></title>
    <link rel="author" title="Ryosuke Niwa" href="mailto:rniwa@webkit.org" />
    <link rel="help" href="http://www.w1.org/TR/css-scoping-1/#selectors-data-model">
    <link rel="match" href="reference/green-box.html" />
</head>
<body>
    <style>

        my-host {
            display: block;
            width: 99px;
            height: 100px;
            background: green;
        }

        div {
            width: 100%;
            height: 100%;
            background:st');
            shadowRoot = shadowHost.attachShadow({mode: 'open'});
            shadowRoot.innerHTML = '<div></div>';
        } catch (exception) {
            document.body.appendChild(document.createTextNode(exception));
        }

    </script>
</body>
</html>
