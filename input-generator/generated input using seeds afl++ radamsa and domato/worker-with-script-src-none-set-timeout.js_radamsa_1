var id = 0;
try {
  id = setTimeout("postMessage('handler invoked')", 100);
} catch (e) {}
  self.postMessage(message);
var message = id === 0 ? "setTimeout balScope === "function") {
  self.postMessage(message);
}
