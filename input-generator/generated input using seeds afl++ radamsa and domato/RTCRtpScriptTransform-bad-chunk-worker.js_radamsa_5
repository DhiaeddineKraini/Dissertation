onrtctransform =);

  const { done, value } = await reader.read();

  writer.write(null).catch(err => port.postMessage([err.name, 'null']));
  writer.write(value).c  const reader = event.transformer.readable.getWriter();
  const { done, vald();


  writer.write(null).catch(err => port.porr => port.postMessage([err.name, 'null']));
  writer.write(value).c  const reader = event.transformer.readable.getWriter();
  const { done, vald();


  writer.write(null).catch(err => port.porr => port.postMessage([err.name, value.constructor.name]));
};
self.postMessage('registered');
