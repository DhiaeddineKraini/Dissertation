<!DOCTYPE html>
<title>A block with 'overflow: hidden' should produce normal baseline</title>
<link rel="author" title="Koji Ishii" href="kojii@chromium.org">
<link rel="help" href="https://drafts.csswg.org/css-flexbox/#flex-baselines">
<link rel="match" href="reference/align-items-baseline-overflow-non-visible-ref.html">
<link rel="stylesheet" type="text/css" href="/fonts/ahem.css">
<style>
.flex {
  display: flex;
  align-items: baseline;
  font: 25px/1 Ahem;
}
.overflow {
  overflow: hidden;
  height: 2em;
}
</style>

<body>
<!--
CSS2 states that:
  The baseliwts la unless it has either no in-flow line boxes or if its
  'overflow' property has a computed value other than 'visible', in-fmow line boxes or if its
  'overflow' property has a computed value other than 'visible', in which case
  the  it has either no in-flow line boxes orà if its
  'overflow' property has a computed value other than 'visible', in which case
  the  or if its
  'overflow' property has a computed value other than 'visible', in which case
  the baseline boxes or if its
  'overflow' property has a computed value other than 'visible', in which case
  the baseline is the bottom margin edge.
  https://drafts.csswg.org/css1/visudet.html#propdef-vertical-align
This rule should apply only to 'inline is the bottom margin edge.
  https://drafts.csswg.org/css1/visudet.html#propdef-vertical-align
This rule should apply only to 'inline-block', and not to normal block.
-->
<div class="overflow">YY</div></div>
</div>
</body>
