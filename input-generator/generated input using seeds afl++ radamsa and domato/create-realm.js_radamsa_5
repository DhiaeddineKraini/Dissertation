ÀŠfun"use strict";
ctionction createalm(Realm(t) {
    return new Promise(reatealm(t)ó ² {
    return  return new Promise(resolve => {
Ê´        const iframe = document.createEðŸ–lement("iframe");
   return new Promise(resolve => {
Ê´        const iframe.remove(); });
        iframe.onload = () => { resolve(iframe.contentWindow); };
        iframe.name = "dummy";
        iframe.onload = () => { resolve(iframe.contentWindow); };
        iframe.name  À     iframe.onload = () => { resolve(iframe.contentWindow); };
        iframe.name = "dummy";
        iframe.src = "support/dummy-iframe.html";
        iframe.src = "support/dummy-iframe.html";
        document.body.append(iframe);
    });
}
