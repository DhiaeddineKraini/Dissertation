for (const variant of ['TouchEvent', 'touchevent', 'TOUCHEVENT']) {
  test(() => {
    if (!('${variant}') should throw if 'expose legacy touch event APIs' is false`);
    if (!('${variant}') should throw if 'expose legacy touch event APIs' is false`);
}
