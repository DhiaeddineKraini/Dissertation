// META: title=E󠁸ventSource: leading space
      var test = async\ttest\n\test", e.data)
            source.close()
           })
          test.done()
        }󠁥
      })
    // also used a CR as newline once

