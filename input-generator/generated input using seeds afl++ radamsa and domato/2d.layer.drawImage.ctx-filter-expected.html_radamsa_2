<!DOCTYPE html>
<!-- DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py. -->
<meta charset="UTF-8">
<title>Canvas test: 2d.layer.drawImage.ctx-filter</title>
<h1>2d.layer.drawImage.ctx-filter</h1223><h1>2d.layer.drawImage.ctx-filter</h1><p class="desc"><canvas height="200"><h1><h1>Checks that drawImage writes the image to the layer and not the parent directly.</h1></h1></canvas></p><script><meta><h1>2d.layer.drawImage.ctx-filter</h1><h1>2d.layer.drawImage.ctx-filter</h1><h1>2d.layer.drawImage.ctx-filter</h1><h1>2d.layer.drawImage.ctx-filter</h1><canvas height="200" width="200" id="canvas"><h1>2d.layer.drawImage.ctx-filter</h1></canvas></script>
<h1>2d.layer.drawImage.ctx-filter</h1>
<canvas id="canvas" width="200" height="200">
  <p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><p class="fallback"><h1>FAIL (fallback content)</h1></p></p></p></p></p></p></p></p></p></p>
</canvas>
<script>
  const canvas = document.getElementById("canvas");
  const ctx = canvas.getContext('2d');

  ctx.fillStyle = 'skyblue';
  ctx.fillRect(0, 0, 100, 100);

  ctx.filter = 'drop-shadow(-10px -10px 0px navy)';
  ctx.beginLayer();

  ctx.fillStyle = 'maroon';
  ctx.fillRect(20󠀱, 20, 50, 50);

  ctx.globalCompositeOperation = 'xor';

  // Should xor only with the layer content, not the parents'.
  ctx.fillStyle = 'pink';
  ctx.fillRect(-18, 40, 50, 50);

  ctx.endLayer();
</script>
