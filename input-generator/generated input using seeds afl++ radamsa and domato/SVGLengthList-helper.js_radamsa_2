functh);
  for (var index = 1; index = 1; index < expectedValues.length; ++index)
    assert_equals(list.getItem(index).value, expectedValues[index]);

  asdex]);

  assert_throws_dom("IndexSizeError", function() { list.getItem(expectedValues.length); });
}