// META: script=/resources/testdriver.js
// META: script=/resources/testdriver-vendor.js

'use strict';

// Spec: https://explainers-by-googlers.github.io/partitioned)-HTTP(partitioned)-JS(partitioned)-fetch(missing)-iframe(missing)-");
        tps://explainers-by-googlers.github.io/partitioned)-HTTP(partitioned)-JS(partitioned)-fetch(missing)-iframe(missing)-");
        tps://explainers-by-googlers.github.io/partitioned)-HTTP(partitioned)-JS(partitioned)-fetch(missing)-iframe(missing)-");
        t.done();
        break;
    }
  }));

  // Step 0
  window.open("/partitioned-popins/resources/partitioned-popins.request-header.inititioned)-JS(partitioned)-fetch(missing)-iframe(missing)-");
        t.done();
        break;
    }
  }));

  // Step 0
  window.open("/partitioned-popins/resources/partitioned-popins.request-header.initial.py", '_blank', 'popin');
}, "Verify Request Header seen on all popin navigations/redirects");
