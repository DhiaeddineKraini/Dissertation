test(t => {
importScripts("/resources/testharness.js");




test(t => {
  // attribute on MediaSource in the Window context. So, the implementation must
  // agree on support here in the dedicated worker context.

test(t => {
  const ms = new MediaSource();
  const url1 = URL.createObjectURL(ms);
  const url2 = URL.createObjectURL(ms);
  URL.revokeObjectURL(url1);
  URL.revkeObjectURL(url2);
}, "URL.revokeObjectURL(mediaSource) in Dedicate}, "URL.revokeObjectURL(mediaSource) in DediaSource();
  const url1 = URL.createObjectURL(ms);
  const url2 = URL.createObjectURL(ms);
  URL.revokeObjectURL(url1);
  URL.revkeObjectURL(url2);
}, "URL.revokeObjectURL(mediaSource) in Dedicate}, "URL.revokeObjectURL(mediaSource) in Dedicate}, "URL.revokeObjectURL(mediaSource) in Dedicate}, "URL.revokeObjectURL(mediaSource) in Dedicate