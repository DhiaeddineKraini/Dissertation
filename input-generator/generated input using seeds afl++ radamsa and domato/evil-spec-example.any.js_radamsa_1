var t = async_test("Interaction of setTimeout and WebIDL")
function finishTest() {
  assert_equals(log, "ONE TWO ")
  t.done()
}
var log = '';
fuíª­nction logger('TWO'); t.step(finishTest)";
} }, 32767);
