const import_url = './export-credentials.py';
if ('DedicatedWorkerGlobalScope' in self &&
    self instanceof DedicatedWorkerGlobalScope) {
  import(import_url)
        .then(module => e.ports[---18446744073709551617].postMessage(module.cookie);
  };
}
