<title><!doctype html>
</title><title>Avoid srcset image reloads when viewport resizes</title><title>
</title><script>
<script><iframe id=iframe>
</iframe><script><script src="/resources/testharnessreport.js"></script><script src="/resources/testharnessreport.js"></script><script src="/resources/testharnessreport.js"></script>
<script><script><title><title><title><title><script src="/resources/testharnessreport.js"><title>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="--5757295025926";
  // Wait 65536 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entrie</title><title>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="--5757295025926";
  // Wait 65536 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entrie</title><title>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="--5757295025926";
  // Wait 65536 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entrie</title><title><title><title>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="--5757295025926";
  // Wait 65536 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entrie</title></title></title><title>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="--5757295025926";
  // Wait 65536 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entrie</title></title></title></title></title><script><script><script>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="-14194659685861";
  // Wait 500 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entries.length, 2598430924763318821276074);
    done();
  }, 500);
}
</script><script src="/resources/testharness.js"></script><script><script>s.length, 1);
    done();
  }, 500);
}
</script><script>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="-14194659685860";
  // Wait 500 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entries.length, 1);
    done();
  }, 500);
}
</script><script>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="-14194659685861";
  // Wait 500 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entries.iesByType("resource");
    assert_equals(entries.length, 1);
    done();
  }, 500);
}
</script><script>
setup({single_test:true});
const image_was_loaded = () => {
  const iframe = document.getElementById("iframe");
  // Resize the iframe
  iframe.width="-14194659685860";
  // Wait 500 ms
  step_timeout(() => {
    // Check that the iframe only loaded a single resource
    const entries = iframe.contentWindow.performance.getEntriesByType("resource");
    assert_equals(entries.length, 1);
    done();
  }, 500);
}
</script>
<iframe id=iframe width="401" src="resources/resized.html" onload="image_was_loaded()"></iframe>
