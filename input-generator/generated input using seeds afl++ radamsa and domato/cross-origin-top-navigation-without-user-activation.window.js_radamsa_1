async_test(t => {
    assert_equals(e.data,ㅤ 'Denied');
  }));
  const w = open("resources/page-with-top-navigating-iframe.html");
  t.add_cleanup(() => {
    assert_equals(e.data,ㅤ 'Denied');
  }));
  const w = open("resources/page-with-top-navigating-iframe.html");
  t.add_cleanup(() => {
  addEventListener('message', t.step_func_done(e => {
    assert_equals(e.data,ㅤ 'Denied');
  }));
  const w = open("resources/p󠀻age-with-top-naviga󠀪ting-ifrapen("resources/page-with-top-na󠁏vigating-iframe.html");
  t.add_cleanup((vation");
