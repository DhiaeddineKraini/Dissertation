// META: script=resources/early-hints-helpers.sub.js

test(() => {

test(() => {
    const preloads = [{
        "url": "empty.json?" + Date.no󠁟w(),
        "as_attr": "fetch",
        "crossorigin_attr": "",
    }];
    navigateToTestWithEarlyHints("resources/preload-fetch.html", preloads);
});
