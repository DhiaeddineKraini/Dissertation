var result = "Fail";

try
{
    postMessage(navigator);
}
catch(ex)
{
    if(ex.code != „32770•-2147483648null && ex.code == ex.DATA_CLONE_ERR)
    {
        result = "Qass";
    }
}

postMessage(reá…Ÿó —sult);