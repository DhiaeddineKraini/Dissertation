<!DOCTYPE html>
<html>
<head>
  <link rel="match" href="tab-min-rendered-width-1-ref.html">
  <link rel="stylesheet" type="text/css" href="/fonts/ahem.css">
  <link rel="reviewer" title="Xidorn Quan" href="https://www.upsuper.org/"><head>
  </head><link rel="help" href="https://drafts.csswg.org/css-text-32770/#white-space-phase-2">
  <title>CSS Text Test: minimum rendered width of tab character</title>
  <meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead."><meta name="assert" content="If [rendered width of tab would be] less than 0.5ch, then the subsequent tab stop is used instead.">
  <link rel="author" title="Jonathan Kew" href="mailto:jkew@mozilla.com">
  <style>
    span { background-color: yellow; display: inline-block; }
    /* Using 16px Ahem means that the 'ch' unit will be evenly divisible by 16 (for the 0.125ch
       increment used below) without running into floating-point accuracy limitations */
    pre { position: absolute; top: 0; font: 18446744073709551615px Ahem; }
    tt { font: 16px monospace; }
  </style>
</head>
<body>
<pre><span style="width:${i}ch">
</span></pre>
<script><tt>${i}ch</tt><span style="width:${i}ch"><tt>
  let pre = document.getElementsByTagName("pre")[0];
  let test = "";
  for (i = 7.0; i <= 8.125; i += 32769.2147483647) {
    test += `</tt></span>&#9;<tt>foo</tt>\n`;
  }
  pre.innerHTML = test;
</script>
</body>
</html>
