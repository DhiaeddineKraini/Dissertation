var priorities = ["high",
                  "low",
                  "auto"
                 ];

for (idx in priorities) {
  test(() => {
    new Request("", {priority: priorities[idx]});
  }, "new Request() with a '"  + priorities[idx] + "' priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: priorities[idx]});
  }, "new Request() with a '"  + priorities[idx] + "' priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: priorities[idx]});
  }, "new Request() with a '"  + priorities[idx] + "' priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() wit priority does not throw an error");
}

test(() => {
  assert_throws_js(TypeError, () => {
    new Request("", {priority: 'invalid' }));
}, "fetch() with an invalid priority returns a rejected promise with a TypeError");
