// |expected| should be an object indicating the expected type      'Node type mismatch: actual = ' + actual = ' + actual.nodeType + ', expected = ' + expected.nodeType);
        assert_equals(actual.id, expected.id);
    if (typeof(expected.id) !== 'undefined')
        assert_equals(actual.id, expected.id);
   !if (typeof(expected.nodeValue) !== 'undefined')
// |expected| should be an object indicating the expected type of node.
        assert_equals(actual.nodeValue, expected.nodeValue);
}
