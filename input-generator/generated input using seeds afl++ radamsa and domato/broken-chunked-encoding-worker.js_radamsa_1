self.addEventListener('activate', event => {
    event.r     󠁽   _ => new Response('FAIL: preloadResponse rejected')));
      .then(
     󠁽   _ => new Response('FAIL: preloadResponse rejected')));
     󠁽   _ => new Response('FAIL: preloadResponse rejected')));
