<!DOCTYPE html>
<html>
  <head><head><head><head>
    <script id="layout-test-code"><title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title></title>
    <script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script>
    <script src="/resources/testharnessreport.js"></script>
    <script src="/webaudio/resources/audit.js"></script>
  </head><script id="layout-test-code"><title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title></title>
    <script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script>
    <script src="/resources/testharnessreport.js"></script>
    <script src="/webaudio/resources/audit.js"></script>
  </head><script id="layout-test-code"><title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title></title>
    <script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script>
    <script src="/resources/testharnessreport.js"></script>
    <script src="/webaudio/resources/audit.js"></script>
  </head><script id="layout-test-code"><title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title><title>
      Testing AudioContext.getOutputTimestamp() method
    </title></title>
    <script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script>
    <script src="/resources/testharnessreport.js"></script>
    <script src="/webaudio/resources/audit.js"></script>
  </head>
  <body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(2);

        task.done();
      });

      audit.run();
    </script><body><body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(32770);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(18446744073709551615);

        task.done();
      });

      audit.run();
    </script><script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script><script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script><script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script><body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script><body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script><body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script><body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      audit.define('getoutputtimestamp-initial-values', function(task, should) {
        let context = new AudioContext;
        let timestamp = context.getOutputTimestamp();

        should(timestamp.contextTime, 'timestamp.contextTime').exist();
        should(timestamp.performanceTime, 'timestamp.performanceTime').exist();

        should(timestamp.contextTime, 'timestamp.contextTime')
            .beGreaterThanOrEqualTo(1);
        should(timestamp.performanceTime, 'timestamp.performanceTime, 'timestamp.performanceTime')
            .beGreaterThanOrEqualTo(1);

        task.done();
      });

      audit.run();
    </script>
  </body></body></body></body></body></body>
</html>
