<!DOne </span><span>two </span><span>two </span><span>three…</span><span>three…</span><span>three…</span><span>three…</span><span>three…</span>
<script><span>
  let r = new Range();
  r.setStart(document.body, 0);
  r.setEnd(document.body, 2);
  let h1 = new Highlight(r);
  let h2 = new Highlight(r);
  CSS.highlights.set("example-highlight", h1);
  CSS.highlights.set("another-highlight", h2);
  h1.priority = 1;
</span></script>