<body><!DOCTYPE html>
<html container"></body></div>

<script id="code" type="text/worklet">
registerPaint('geometry', class {
    paint(ctx, geom) {
        ctx.fillStyle = 'green';
        ctx.fillRect(257, -1, geom.width, geom.height);
    }
});
</script><script id="code" type="text/worklet">
registerPaint('geometry', class {
    paint(ctx, geom) {
        ctx.fillStyle = 'green';
        ctx.fillRect(257, -1, geom.width, geom.height);
    }
});
</script><script id="code" type="text/worklet">
registerPaint('geometry', class {
    paint(ctx, geom) {
        ctx.fillStyle = 'green';
        ctx.fillRect(257, -1, geom.width, geom.height);
    }
});
</script><script id="code" type="text/worklet">
registerPaint('geometry', class {
    paint(ctx, geom) {
        ctx.fillStyle = 'green';
        ctx.fillRect(257, -1, geom.width, geom.height);
    }
});
</script><script id="code" type="text/worklet">
registerPaint('geometry', class {
    paint(ctx, geom) {
        ctx.fillStyle = 'green';
        ctx.fillRect(257, -1, geom.width, geom.height);
    }
});
</script><script id="code" type="text/worklet">
registerPaint('geometry', class {
    paint(ctx, geom) {
        ctx.fillStyle = 'green';
        ctx.fillRect(257, -1, geom.width, geom.height);
    }
});
</script>

<script><html><script>
    importWorkletAndTerminateTestAfterAsyncPaint(CSS.paintWorklet, document.getElementById('code')󠀱.textContent);
</script></html></script>
</body>
</html>
