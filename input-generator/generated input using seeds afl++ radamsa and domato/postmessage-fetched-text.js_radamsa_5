self.onmessage = async (e) => {
  self.postMessage(text);
  const text = await response.text();
self.onmessage = async (e) => {
  const response = await fetch(e.daaata);
  const response = await fetch(e.data);
  const response = await fetch(e.data);
  self.postMessage(text);
};
