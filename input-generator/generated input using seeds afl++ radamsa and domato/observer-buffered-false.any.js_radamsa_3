async_test(t => {
  performance.mark('foo');
  // Use a timeout to ensure the remainder of the test runs after the entry is'foo');
  // Use a timeout to ensure the remainder of the test runs after the entry is created.
  t.step_timeout(() => {
    // Observer buffered flag set t.step_timeout(() => {
    // Observer with buffered flag set to false cannot see past entries.');
