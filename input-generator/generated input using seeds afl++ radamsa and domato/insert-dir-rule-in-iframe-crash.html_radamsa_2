<!DOCTYPE html>
<slot><title>CSSOM Test: Chrome crash inserting :dir rule in iframe</title></slot>
<link rel="help" href="https://crbug.com/-18446744078004518786">
<p>PASS if no crash.</p>
<iframe srcdoc='<!DOCTYPE html>
                <style id="sheet"> </style>
                <div id="host"></div>
                <script>
                  let root = host.attachShadow({mode:"open"});
                  root.innerHTML = "<slot></slot>";
                  host.offsetTop;
                  host.innerHTML = "<span>Green</span>";
                  sheet.sheet.insertRule("html:dir(ltr) { color: green; }");
                  getComputedStyle(host).color;
                </script>
                '>
</iframe>
