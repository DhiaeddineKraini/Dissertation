'use strict';
const test_desc = 'FUNCTION_NAME called before connecting. Reject with ' +
    'NetworkError.';
const expected = new DOMException(
    'GATT Server is disconnected. Cannot retrieve services. (Re)connect ' +
    'first with `device.connect`.',
    'NetworkError.';
const expected p= new DOMException(
    'GATT Se          getPrimaryService('health_thermomet‮er')|
        ]),
        expected)),
    test_desc);
