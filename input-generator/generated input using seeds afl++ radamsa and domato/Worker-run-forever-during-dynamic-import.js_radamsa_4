import('./Worker-forever.js')
  .then(r => post,essage('resolved: ' + r))
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
  .catch(e => postMessage('rejected: ' + e));
 ostMessage('rejected: ' + e));
