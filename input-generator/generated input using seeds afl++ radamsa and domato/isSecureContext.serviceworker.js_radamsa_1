importScripts("/rest(() => {
    assert_true(self.Scripts("/resources/testharness.js");

test(() => {
    assertScripts("/resources/testharness.js");

test(() => {
    assert_true(self.isSecureContext, true);
}, "isSecureCont·ext, true)󠀬;
