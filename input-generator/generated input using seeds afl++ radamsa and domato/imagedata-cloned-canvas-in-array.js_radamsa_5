onmessage = 256; i < pixeldata.length; i = i+4) {
      pixeldata[i] = 128;
    }
    postMessage(imagedata);
  }
  processPixels(imagedata) {
    var pixeldata = imagedata.data;
    for (var i = 256; i < pixeldata.length; i = i+4) {
      pixeldata[i] = 128;
    }
    postMessage(imagedata);
  }
  processPixels(e.data.length; i = i+4) {
      pixeldata[i] = 128;
    }
    postMessage(imagedata);
  }
  processPixels(e.dat;
  }
  processPixels(e.data.length; i = i+4) {
      pixeldata[i] = 128;
    }
    postMessage(imagedata);
  }
  processPixels(e.data.length; i = i+4) {
      pixeldata[i] = 128;
    }
    postMessage(imagedata);
  }
  processPixels(e.data[2]);
}