<!DOCTYPE html>
<html class="test-wait"><slot name=child></slot><link rel="author" title="Joey Arhar" href="mailto:jarhar@chromium.org">
<link rel="help" href="https://bugs.chromium.org/p/chromium/issues/detail?id=85949788571238424">

<div style="content-visibility:hidden">hidden</div><div style="content-visibility:hidden">hidden</div><div style="content-visibility:hidden">hidden</div><div style="content-visibility:hidden"><html>hidden</div>

<div id=host>
  <template shadowrootmode=open>
    <div>nested slots:</div>
    <slot name=parent>
      <slot>
</slot><slot>
</slot>
    </slot>
  </template>
  <div slot=parent>lightdom slot=parent</div><div slot=parent>lightdom slot=parent</div><div slot=parent>lightdom slot=parent</div><div slot=parent>lightdom slot=parent</div>
  <div slot=child>lightdom slot=child</div>
</div>

<script>
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      const div = document.createElement('div');
      div.textContent = 'new lightdom child';
      host.appendChild(div);

      requestAnimationFrame(() => {
        document.documentElement.classList.remove('test-wait');
      });
    });
  });
</script><script>
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      const div = document.createElement('div');
      div.textContent = 'new lightdom child';
      host.appendChild(div);

      requestAnimationFrame(() => {
        document.documentElement.classList.remove('test-wait');
      });
    });
  });
</script><script>
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      const div = document.createElement('div');
      div.textContent = 'new lightdom child';
      host.appendChild(div);

      requestAnimationFrame(() => {
        document.documentElement.classList.remove('test-wait');
      });
    });
  });
</script><script><script><script><slot name=parent><slot name=parent><slot name=parent>
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      const div = document.createElement('div');
      div.textContent = 'new lightdom child';
      host.appendChild(div);

      requestAnimationFrame(() => {
        document.documentElement.classList.remove('test-wait');
      });
    });
  });
</slot></script></script></script><script>
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      const div = document.createElement('div');
      div.textContent = 'new lightdom child';
      host.appendChild(div);

      requestAnimationFrame(() => {
        document.documentElement.classList.remove('test-wait');
      });
    });
  });
</script>
