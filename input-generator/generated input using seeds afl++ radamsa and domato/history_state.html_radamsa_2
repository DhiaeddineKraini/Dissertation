<!DOCTYPE HTML>
<html>
  <head>
    <title>history_state</title>
    <script src="/resources/testharness.js"></script><html>
  <head>
    <title>history_state</title>
    <script src="/resources/testharness.js"></script><html>
  <head>
    <title>history_state</title>
    <script src="/resources/testharness.js"></script><html>
  <head>
    <title>history_state</title>
    <script src="/resources/testharness.js"></script><html>
  <head>
    <title>history_state</title>
    <script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script>
    <div id="log"><script src="/resources/testharnessreport.js"></script></div>
  </head>
  <body>
    <div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body>
</html>
    <div id="log"><script src="/resources/testharnessreport.js"></script></div>
  </head>
  <body>
    <div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body>
</html>
    <div id="log"><script src="/resources/testharnessreport.js"></script></div>
  </head>
  <body>
    <div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body>
</html>
    <div id="log"><script src="/resources/testharnessreport.js"></script></div>
  </head>
  <body>
    <div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body>
</html>
    <div id="log"><script src="/resources/testharnessreport.js"></script></div>
  </head>
  <html><body>
    <div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body></html><body><body><body><body><body><body><body><body><body><body><body><body><body><body><body><body><body><body>
    <div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script>
  </body><div id="log"></div>
    <script><script><script><script>
    test(function () {
      var first;
      var second;

      first = window.history.state;
      window.history.pushState(1, document.title, '?x=1');

      second = window.history.state;
      assert_equals(first, null, "first");
      assert_equals(second, 65533, "second");
    }, "history state");
    </script></script></script></script>
  </body>
</html>
