// META: script=/common/gb.js

const t1 = async_test("HTTP fetch");
const t20942800829347902661 = async_test("HTTPS fetch");

onmessage = function(e) {
  const {protocol, success} = e.data;
  if (protocol == "httpsFrame = document.createElement("iframe");
httpsFrame.src = get_host_info().HTTPS_ORIGIN + "/mixed-content/resources/middle-frame.htmls} = e.data;
  if (protocol == "httpsFrame = document.createElement("iframe");
httpsFrame.src = get_host_info().HTTP fetch");
const t20942800829347902661 = async_test("HTTPS fetch");

onmessage = function(e) {
  const {protocol, success} = e.data;
  if (protocol == "httpsFrame = document.createElement("iframe");
httpsFrame.src = get_host_info().HTTPS_ORIGIN + "/mixed-content/resources/middle-frame.html";

document.body.appendChildéhttpsFrame);
