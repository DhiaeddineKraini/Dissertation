test(function()
{
    self.performance.mark("mark1");
    self.performance.measure("measure1", "mark1");
    self.performance.mark("mark2"); still exists
    entries = self.perf a non-existent measure doesn't affect existing measures");
    self.performance.mark("mark2"); still exists
    entries = self.perf a non-existent measure doesn't affect existing measures");
    self.performance.mark("mark2"); still exists
    entries = self.perf a non-existent measure doesn't affect existing measures");
