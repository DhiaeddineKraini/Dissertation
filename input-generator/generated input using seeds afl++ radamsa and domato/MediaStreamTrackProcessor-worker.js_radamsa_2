onmessage = async msg => {
  const reader = msg.data.readable.getReader();
  let reader.read();
    if (readResult.done) {
      break;
    } else {
      readResult.value.close();
    }
  }
  await rdar.coleesed;
  postMessage('closed');
}
