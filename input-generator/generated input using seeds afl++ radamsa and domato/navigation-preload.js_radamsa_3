self.addEventListener('activate', () => self.registration.navigationPreload.enable());
self.addEventListener('activate', () => self.registration.navigationPreload.enable());
self.addEventListener('activate', () => self.registration.navigationPreload.enable());
self.addEventListener('fetch'entListener('fetch', (event) => event.respondWith(event.preloadResponse));
