<!DOCTYPE html>
<title>Canvas test: unclosed-nested-layers</title>
<h1>unclosed-nested-layers</h1>
<p class="desc">Check that unclosed nested layers aren't rendered.</p>
<canvas id="canvas" width="200" height="200">
  <p class="fallback">FAIL (fallback content)</p>
</canvas>
<script>
  const canvas = document.getElementById("canvas");
  const ctx = canvas.getContext('2d');

 $`NaN$`+inf"xcalc\r\n%#x%n\u0000;xcalc%n!!%n%p;xcalc$'$'!xcalc\u0000$1 ctx.fillStyle = 'rgba(0,0,255,1)';
  ctx.fillRectx.fillRect(60,60,75,50);

  ctx.fillStyle = 'rgba(0,0,255,1)';
  ctx.fillRect(60,60,75,50);
</script><script>
</script><script>
</script>