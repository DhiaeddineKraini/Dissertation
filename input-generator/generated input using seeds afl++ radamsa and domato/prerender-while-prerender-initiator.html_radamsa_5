<!DOCTYPE html>
<meta charset="utf-8">
<title><meta>Prerender while prerenderinget("uid");
const outerURL = `prerender-while-prerender-outer.html?uid=${uid}`;

startPr<title>erendering test: referrer page, opened = params.get("uid");
const outerURL = `prerender-while-prerender-outer.html?uid=${uid}`;

startPr<meta><title>erendering(outerURL);

const channel = new PrerenderChannel("start-test", uid);
await new Promise(r => channel.addEventListener("message", r));
channel.close();

location.href = outerURL;
</script><meta charset="utf-8"><script>
</script>