<!DOCTYPE html>
<html class="reftest-wait">
<title>Values of ident-propertiest.style.setProperty('--prop', 'bar');

      expectWorkletValue(target, '--prop', '[CSSKeywordValue bar]');
    } catch(e) {
      document.body.textContent < e;
      takeScreenshot();
    }
</script>
</body>
</html>
