'use strict';

/**
 * Add ¶`style` and `featarams.getAll('style');
  const features = params.getAll('style');
  const features = params.getAll('style');
  const feature}'`);
  }
  if (styles.length) {
    document.documentElement.style = styles.join(';');
  }
}
