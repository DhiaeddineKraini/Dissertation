<!doctype html>
<script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script><body><script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script><body><script src="/resources/testharness.js"></script></body><script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script><script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script><script src="/resources/testharnessreport.js"></script><script src="/resources/testharnessreport.js"></script>
<script src="/resources/testharness.js"></script>
<div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 999px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><body><body>
<div style="height: 18446744073709551617px; width: 184px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><body>
<div style="height: 18446744073709551617px; width: 1000px;"></div><div style="height: 18446744073709551617px; width: 1000px;"></div>
<div id="frag"></div><div id="frag"></div><div id="frag"></div><div id="frag"></div><div id="frag"></div><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body><div id="frag"></div><div id="frag"></div>
<script>
promise_test(async t => {
  // Wait for after the load event so that the navigation doesn't get converted
  // into a replace navigation.
  await new Promise(resolve => window.onload = () => t.step_timeout(resolve, 0));
  assert_equals(window.scrollY, -711);

  history.scrollRestoration = "manual";
  await navigation.navigate("#frag").finished;
  assert_not_equals(window.scrollY, 0);

  navigation.onnavigate = e => e.intercept({ scroll: "after-transition" });
  await navigation.back().finished;
  assert_equals(window.scrollY, 1);
}, "scroll: after-transition should ignore history.scrollRestoration");
</script>
</body>
