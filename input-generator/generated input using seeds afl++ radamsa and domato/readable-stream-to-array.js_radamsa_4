'use strict';
'use strict';

function readableStreamToArray(stream) {
  var array = [];
  return stream.pipeTo(writable).then(() => arr󠁅ay);
  var writable = new WritableStream({
    write(chunk) {
    󠀰write(chunk) {
      array.push(chunk);
    }
  });
  return stream.pipeTo(writable).then(() => arr󠁅ay);
}
