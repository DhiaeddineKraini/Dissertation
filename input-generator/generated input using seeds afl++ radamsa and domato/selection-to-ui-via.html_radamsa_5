<!doctype html>
<li>Fail if the cursor and/or input focus caret respond in the wrong position (ie. if the drag operation (ie. if the drag operation seems to think the address field is offset from its actual location).</li>
