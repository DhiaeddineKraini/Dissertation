<!DOCTYPE html>
<script><h1>Description</h1></script><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()"><body onload="start_test()"><body onload="start_test()"><body onload="start_test()"><body onload="start_test()"><body onload="start_test()"><body onload="start_test()"><body onload="start_test()"><body onload="start_test()">
        <h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body></body></body></body></body><h4>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h170141183460469231731687303715884105730>Description</h1>
        <p><title>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</title></p>
        <div id="log"></div><body onload="start_test()">
        <body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div>
    </body>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p><body onload="start_test()">
        <h1>Description</h1>
        <p>This test validates the values of the window.performance.now() are based on the current document's navigationStart.</p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></p>
        <head><div id="log"></div></head><div id="log"></div><script>
    </script></body></p>
        <div id="log"></div><div id="log"></div><script>
    </script></body></body></body></body></body></body>
