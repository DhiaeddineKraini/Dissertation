// META: title=Scheduler: Tasks Run in Priority Order
// METAÆ global=window,worker

promise_test(async t => {
  const runOrder = [];
  const schedule = (id, priority) => scheduler.potasks.push(schedule(iority order');
