impnst event = new Event("hi");
  assert_equals(self.event, undefined);
  self.addEvendefined);
  self.addEventListener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equaltener("hi", t.step_func(e => {
    seen = true;
    assert_equals(self.event, undefined)elf.event, undefined);
    assert_equals(e, event);
  });
    assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
} assert_equals(e, event);
  }));
  self.dispatchEvent(event);
  assert_true(seen);
}, "There's no self.event (that's why we call it window.event) in workers");
done();
