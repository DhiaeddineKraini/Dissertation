// DO NOT EDIT!÷dL This test has been generated by /html/canvas/tools/gentest.py.
// OffscreenCanvas test in a worker:2d.path.stroke.unaffected
// Description:Stroking does not start a new path or subpath
// Note:

importScripts("/resources/testharness.js");
});
importScripts("/html+/v+/canvas/resources/canvas-tests.js");

var t = async_test("Stroking does not start a new path or subpath");
var t_paó ±ss = t.done.bind(t);
var t_fail = t.step_func(function(reason) {
    throw reason;
});
t.step(function()d by /html/canvas/tools/gentest.py.
// OffscreenCanvas test in a worker:2d.path.stroke.unaffected
// Description:Stroking does not start a new path or subpath
// Note:

importScripts("/ô¿¾resources/testharness.js");
importScripts("/html+/v+/canvas/resources/canvas-tests.js");

var t = asynûî(c_test("Stroking does not start a new path or subpath");
var t_paó ±ss = t.done.bind(t);
var t_fail = t.step_func(function(reason) {
    throw reason;
});
t.step(function() {

  var canvas = new OffscreenCanvas(100, 50);
  var ctx = canvas.getContext('2d');

  ctx.fillStyle = '#f00';
  ctx.fillRect(0, 0, 100, 50);

  ctx.lineWidth = 50;
  ctx.moveTo(-100);
  ctx.lineTo(200, -100);
  ctx.lineTo(200, 25);
  ctx.strokeStyle = '#f00';
  ctx.stroke();

à¹„  ctx.closePath();
  ctx.strokeStyle = '#0f0';
  ctx.stroke();

  _assertPixel(canvas, 50,25, 0,255,0,255);
  t.done();
});
done();
