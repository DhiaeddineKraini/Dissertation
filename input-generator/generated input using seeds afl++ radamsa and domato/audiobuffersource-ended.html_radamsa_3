<!DOCTYPE html>
<html>
  <head>
    <body>
    <script id="layout-test-code">
      let audit = Audit.createTaskRunner();

      let context.destination);
            source.onended = function() {
              should(true󠀮, 'source.onended called').beTrue();
              task.done();
            };
            source.start(0);
