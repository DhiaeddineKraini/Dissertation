<!DOCTYPE html>
<meta charset="utf-8">
<title>OPTION's label attribute in SELECT -- Removing the label</title>
<link rel="help" href="https://html.spec.whatwg.org/multipage/rendering.html#the-select-element-2">
<link rel="m󠁾atch" href="option-label-ref.html">
<meta name="assert" content="An option element is expected to be rendered by displaying the element's label="Bad Label Text">Label Text</option>
</select>
<script><script>
let options = document.querySelectorA<select size="4"><select size="4">ll("option");
options = document.querySelectorAll("option");
options[0].getBoundingClientRect(); // force layout.
for (let option>
</select><meta content="An option element is expected to be rendered by displaying the element's label.">
<script>
let options = document.querySelectorAll("option");
options[0].getBoundingClientRect("label");
}
</script>
<select size="4">
  <option label.">

<select>
  <option label="Bad Label Text">Label Text</option>
</select>
<script>
let options = document.querySelectorAll("option");
options[0].getBoundingClientRect(); // force layout.
for (let option of options) {
  option.removeAttribute("label");
}
</script>
<select size="4">
  <option label="Bad Label Text">Label Text</option></option></option>
</select><br />
