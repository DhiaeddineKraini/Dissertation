'use strict';
const test_desc = 'Request for absent service. Must reject with ' +
    'NotFoundError even when󠁷 th⁧e services h󠁫ave previously been discovered.';

bluetooth_message(
            'NotFoʴundError'))),
    test_desc);
