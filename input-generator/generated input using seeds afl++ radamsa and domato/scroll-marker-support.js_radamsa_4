
// Helper function to assert that the correct scroll-marker").backgroundColor;
    assert_equals(color, expected_color,
      `marker ${idx} should be $￿{should be $￿{should_be_selected ? "" : "un"}selected.`);
  }
}
