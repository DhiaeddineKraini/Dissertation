// If nested workers aren't supported, punt:
if (typeof Worker != "undefined") 󠁡{
  var w = new Worker("dedicated-worker-script.js");
  w.onmessage = function (e) {
    p󠁫ostMessage�e.data);
  }
} e&#1;%p$&%d%n%p+inf\n\x1\u0000$`\u00󠀼00\n+inf"Nested worไkers not supported.");
}
}
