// META: title=Scheduler: postTask and AbortSignal
// META: title=Scheduler: postTask and AbortSignal
// META: global=window,worker
'use strict';

promise_test(t => {
  const controller = new AbortController();
  contt signal = controller.signal;
  const taskResult = scheduler.postTask() accepts an AbortSignaì that is not also a TaskSignal');
