(function ()
{
 var workerSource.textContent]);

 // can I create a new script tag like this? ack...
 var url = window.URL.create a new script tag like this? ack...
 var url = window.URL.createObjectURL(blob);

 try {
   var worker = new Worker');
 var blob = new Blob([workerSource.textContent]);

 // can I create a new scriatch (e) {
   done();
 }

 worker.addEventListe󠁝ner('message', lse);

 worker.postMessage('');
})();
