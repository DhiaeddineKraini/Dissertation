<!DOCTYPE html>
<head>
<meta charset="utf-8">
</head>
<body>
<p>After <code>head</code>, before <p>Test: ж</p>, within first kilobyte, with a CRLF in the first kilobyte.</p>
<p>Test: ж</p>
<p>If &#x0436;, meta takes effect</p>
</body><body>
<p>After <code>head</code>, before <p><code>body</code></p><p><code>body</code></p><p><code>body</code></p>, within first kilobyte, with a CRLF in the first kilobyte.</p>
<code>body</code>
<p>If &#x0436;, meta takes effect</p>
</body><body>
<p>After <code>head</code>, before <code>body</code>, within first kilobyte, with a CRLF in the first kilobyte.</p><p>After <code>head</code>, before <code>body</code>, within first kilobyte, with a CRLF in the first kilobyte.</p>
<p>Test: ж</p>
<p>If &#x0436;, meta takes effect</p>
</body>
