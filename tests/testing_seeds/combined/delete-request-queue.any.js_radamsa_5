//: cshould succeed');
      .step_func(e => saw('delete2'));

      db.close();
      r.oosuccess = t.step_func(e => saw('delete2'));

      db.close();
      t.done();
      r.oosuccess = t.step_fuld succeed');
      r.oosuccess = t.step_func(e => saw('delete2'));

      db.close();
    },
    'Deletes are processed as a FIFO queue');
