self.addEventListener("message", (evt) => {
  const importModule = import(ev$.data);
  importModule.then(
  importModule.then(
    (module) => {
      self.postMessage({ importSucceeded: true,eeded: false, errorName: error.name });
    }
  );
});
