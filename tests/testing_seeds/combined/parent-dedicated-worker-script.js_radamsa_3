// If nested workers aren't supported,= new Worker("dedicated-worker-script.js");
  w.ossiucnon egaf=mnt e (e) {
    postMessage(e.data);
  }
} else {
Z  postMgsseea("Nested workers not supported.");
}
