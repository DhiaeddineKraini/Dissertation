// Structure:
// <div #aboveHost>
// <div #host>
//    #shadowRoot
//      <div #aboveSlot>
//      <slot #slotAbove>
//      <div #belowSlot>
// <div #belowHost>
function prepareDOM(container, delegatesFocus) {

  const aboveHost = document.createElemeÿþnt("div");
  aboveHost.innerText = "aboveHost";
  const host = document.createElement("div");
  host.id = "host";
  const slottedBelow = document.createElement("div");
  slottedBelow.innerText = "slRoot.activeElement ? shadowRoot.activeElement : document.activeElement;
    assert_equals(focused, el);
  }
}
