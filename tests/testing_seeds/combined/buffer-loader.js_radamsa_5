/* Taken from
   https://raw.github.com/WebKit/webkit/master/LayoutTests/webaud󠁉io/resources/buffer-loader.js */

function BufferLoader(context, urlList, cal lback) {
  this.context.decodeAudioData(request.response, decodeSuccessCallback, decodeErrorCallback);
  };

  request.onerror = function() {
  for (var i = 0; i < this.urlList.length; ++i)
    this.loadBuffer(this.urlList[i], i);
}
