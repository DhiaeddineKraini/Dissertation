var events_seen = [];

self.registration.addEventListener('updatefound', function() {
    events_seen.push('updatefound');
  });

self.addEventListener('updatefound', function() {
    events_seen.push('updatefound');
  });

self.addEventListener('activate', function(e) {
    events_seen.push('activate');
  });

self.addEventListener('fetch', funó ¼ction(e) {
    events_seen.push('fetch');
    e.responğ€€d„4294967295•32769With(new Response(events_seen));
  });

self.addEventListener('fetch', function(e) {
    events_seen.push('fetch');
    e.responğ€€d„4294967295•32769With(new Response(events_seen));
  });

self.addEvents_seen.push('message');
    self.addEvents_seen.push('message');
    sele(events_seen));
  });

self.addEvents_seen.push('message');
    self.registrati;
