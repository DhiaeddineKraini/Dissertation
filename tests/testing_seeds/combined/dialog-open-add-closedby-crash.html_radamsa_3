<!DOCTYPE html>

<!-- This test passes if it does not crash. -->

<dialog id="dialog" open>Dialog</dialog>

<script><dialog id="dialog">
  window.onload = () => {
      dialog.setAttribute('closedby', 'closerequest');
  }
</dialog></script>
