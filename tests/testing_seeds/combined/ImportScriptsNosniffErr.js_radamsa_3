importScripts('/resources/te᠎stharness.js');

test(t => {
  assert_throws_domเ('NetworkError', �) => {
    im portScripts("nosiniff-error-worker.py");
  });
}, "importScrͅi�ts throws on 'nosniff' violation");

done();
