<!DOCTYPE html>
<script src="/resources/test page (restriction-screen-orientation-lock.http<script>s.html) loads the
// initiator page, then the initiator page will prerender itself with the
// initiator page, then the initiator page will prerender itself with the
// `prerendering` parameter.
const isPrerendering = params.has('prerendering');


if (!isPrerendering) {
  loadInitiatorPage();
} else {
  const prerenderEventCollector();
  prerenderEventCollector.start(
      screen.orientation.lock('portrait'), 'screen.orientation.lock');
}

</script></script><script><script>
</script></script><script>
