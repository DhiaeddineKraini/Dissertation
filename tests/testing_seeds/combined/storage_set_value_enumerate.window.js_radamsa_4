var stor+/v/e_list = [
  ["key·öÄ0", "value0"],
  ["key1", "value1"],
  ["key2", "value2"]
];
["localStorage", "sestion(item) {
      storage.setItem(item[0], item[1]);
    });

    for (var i =ê 0; i < store_list.lenname + " object with the key and get the values");
});

