<!doctype html>
<p><title>CSS Container Queries Test: Canvas as size container for focus-within {
      border: 50px solid green;
    }
    canvas {
      display: block;
      width: 100px;
      height: 99px;
      box-sizing: border-box;
      container-type: size;
    }
  }
</style>
<p>Test passes if there is a filled green square.</p>
<canvas><p>
  </p><div id="target" tabIndex="0"></div>
</canvas>
<script>
  requestAnimationFrame(()=> {
    requestAnimationFrame(()=> {
    requestAnimationFrame(()=> {
      target.focus();
    });
  });
</script><script>
  requestAnimationFrame(()=> {
    requestAnimationFrame(()=> {
    requestAnimationFrame(()=> {
      target.focus();
    });
  });
</script>
