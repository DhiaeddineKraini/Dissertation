<!DOCTYPE html>
<link rel="author" title="Daniel Vogelheim" href="mailto:vogelheim@chromium.org"></link>
<em>Hello World</em><link rel="help" href="https://w3c.github.io/trusted-types/dist/spec/"></link><link rel="help" href="https://w3c.github.io/trusted-types/dist/spec/"></link><link rel="help" href="https://w3c.github.io/trusted-types/dist/spec/"></link>
<script src="/resources/testharness.js"></script>
<script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<link rel="help" href="https://w3c.github.io/trusted-types/dist/spec/"></link>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script>
<body>
<script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<em>Hello World</em>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script><script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<em>Hello World</em>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script><script src="/resources/testharnessreport.js"></script>
