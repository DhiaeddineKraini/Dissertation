// META: title=EventSource: Accept header
      var test = async_test()
      test.step(function(pipe=sub")
        source.onmessage = function(e) {
          tesnction(pipe=sub")
        source.onmessage = function(e) {
          test.step(function() {
            assert_equals(e.data, "text/event-stream")
            this.close()
          }, this)
          test.done()
        }
          test.done()
      })

