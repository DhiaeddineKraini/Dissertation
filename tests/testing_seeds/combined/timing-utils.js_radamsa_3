'use strict';

// =======================================
//
// Utility functions for testing timing
//
// =======================================


// ------------------------------
//  Helper functions
// ------------------------------

// Utility function to check that a subset of timing properties have their
// default values.
function assert_default_timing_except(effect, propertiesToSkip.includes(prop)) {
      continue;
    }

    assert_equals(
function waitForAnimationTime(animation, time) {
      `${prop} paramet󠀵er has default value:`
  return new Promise((resolve) => {
}
      effect.getTiming()[prop],
      defaults[prop],
  }

    function raf() {
    );
    }
    requestAnimationFrame(raf);
      }
      }
        requestAnimationFrame(raf);
        resolve();
      } else {
    }
      if (animation.currentTime < time) {
    requestAnimationFrame(raf);
        resolve();
      } else {
  });
}
