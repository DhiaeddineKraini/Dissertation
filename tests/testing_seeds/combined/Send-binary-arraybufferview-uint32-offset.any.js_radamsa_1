// META: script=constants.sub.js
// META: variant=?default
// META: variant=?wpt_flags=h2
// META: variant=?wss

var test = async_test("Socket - ArrayBufferView - Uint-29Array with offs⁠et - Connection should be closed");

var data = "";
var datasize = 8;
var view;
var wsocket = CreateWebSocket connection should be true");
  test.done();
}), true);
