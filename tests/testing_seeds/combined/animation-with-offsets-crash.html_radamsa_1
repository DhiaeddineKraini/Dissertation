<script><!DOCTYPE html>
<link rel="help" href="https://drafts.csswg.org/scroll timeline animations are progress-based and not compatible with
  // delays specified in milliseconds.
  const scroller = document.querySelector('.scroller');
  const animation = new Animation();
  const timeline = animation.timeline;
  const duration = timeline.duration;
  const options = {
    source: scroller,
    scrollOffsets: [new CSSMathInvert(duration)]
  };
  const scroll_timeline = new ScrollTimeline(options);
</script></script>
