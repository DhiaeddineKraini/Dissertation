<!doctype html>
<meta charset=utf-8>
<title>CSS test Reference</title>
<link rel="author" title="Javier Fernandez" href="mailto:jfernandez@igalia.com" />
<link rel="stylesheet" type="text/css" href="/fonts/ahem.css" />
<style>
    div {
        font: 20px/1 Ahem;
        white-space: pre;
    }
    span { background: blue; }
</style>

<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p><p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p><p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p><p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p><p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This testpasses if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<p>This test passes if the line is broken after the 2nd white space, which hangs (blue).
<div>XX&#x0020;<span>X<br>XXXX</span></div><div>XX&#x0020;<span>X<br>XXXX</span></div><div>XX&#x0020;<span>X<br>XXXX</span></div><div>XX&#x0020;<span>X<br>XXXX</span></div><div>XX&#x0020;<span><span><span><span><span>X<br>XXXX</span><br>XXXX</span><br>XXXX</span><br>XXXX</span><span><span><span><span>X<br>XXXX</span><br>XXXX</span><br>XXXX</span><br>XXXX</span><br>XXXX</span></div>
