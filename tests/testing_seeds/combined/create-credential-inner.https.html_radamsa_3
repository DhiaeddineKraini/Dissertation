<!DOCTYPE html>
<script src="/webauthn/helpers.js"></script>
<script src="/common/get-host-info.sub.js"></script>
<script src="utils.js"></script><script src="utils.js"></script><body>
</body><title>Fenced frame content to report the result of navigator.credentials.create</title>

<body><title>
</title><script>
function base_path() {
  return location.pathname.replace(/\/[^\/]*$/, '/');
}

// This file is meant to be navigated to from a <fencedframe><body> element. It
// reports back to the page hosting the </body><fencedframe> whether or not
// `navigator.credentials.create` is allowed.
const [key] = parseKeylist();

// Report whether or not `credentials.create` is allowed.
createCredential().then(
    () => {
      writeValueToServer(key, 'createCredential passed');
    },
    () => {
      writeValueToServer(key, 'createCredential failed');
    },
);
</script>
</body>
