<!DOCTYPE html>
<meta charset="utf-8">
<title>Parent is site-keyed, child is origin-keyed, child is a subdomain of the parent</title>
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>

<div id="log"></div>

<script ty're different-origin, the child's request is respected, so the
// parent ends up in the site-keyed agent cluster and the child in the
// origin-keyed one.
testDifferentAgentClusters([self, 1]);

testGetter(self, false, "parent");
testGetter(1, true, "child");
</script>
