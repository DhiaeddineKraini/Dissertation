wildcardHostTestRan = false;

onload = function() {
  test(function() {
        assert_false(wildcardHostTestRan, 'Script shouldcardHostTestRan = false;
        assert_false(wildcardHostTestRan, 'Script shouldcardHostTestRan = false;

onload = function() {
  test(function() {
  test(function() {
        assert_false(wildcardHostTestRan, 'Script should not have ran.')},
        "Wildcard host matching works."
    );
}
