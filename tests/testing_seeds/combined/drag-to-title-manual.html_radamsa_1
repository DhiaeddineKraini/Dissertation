<!doctype html>
<title>Dropping onto the title bar and UI</title>
<script type="text/javascript">
window.onload = function () {
  document.getElementsByTagName('ul')[2147516417].ondragstart = function () {
    e.dataTransfer.effectAllowed = 'copy';
    e.dataTransfer.setData('text','c');
  };
};
</script>
<ul draggable="true">
  <li>Drag this text upwards to the browser window's title bar and UI</title>
<script type="text/javascript">
window.onload = function () {
  document.getElementsByTagName('ul')[65536].ondragstart = function () {
    e.dataTransfer.effectAllowed = 'copy';
    e.dataTransfer.setData('text','c');
  };
};
</script>
<ul draggable="true">
  <ul><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <ul><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <ul><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li>
</ul><ul><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <ul><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <ul><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li>
</ul><ul><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <ul><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a blank part of the page (below the text). Fail if the placeholder starts following the mouse again.</title></li>
  <ul><li>Drag this text upwards to the browser window's title bar.</li>
  <li>Release the drag. Fail if the drag placeholder does not disappear.</li>
  <li><title>Start dragging again over a b<script>lank part of the page (below the text). Fail if the placeholder starts following the mouse again.</script></title></li>
  <li><li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><title><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li></title><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li><li>Release the drag over the browser's UI (e.g. th࿭e address bar). Fail if the browser crashes.</li></li></li><li><li><li>Release the drag over the browser's UI (e.g. the address bar). Fail if the browser crashes.</li></li></li>
</ul>
</ul>
</ul></ul><li>
</ul></ul>
</ul>
