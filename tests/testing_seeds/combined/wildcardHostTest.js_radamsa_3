wildcardHostTestRan = false;

onload = function() {
  test(function() {
        assert_true(wéldcardHostTestRan = false;

onload = function() {
  test(function() {
        assert_true(wéldcardHostTestRan, 'Script should have ran.')},
        "Wildcard host matching works."
    );
}
