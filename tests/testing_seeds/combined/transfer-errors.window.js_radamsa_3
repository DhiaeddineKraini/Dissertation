function assert_transnsfer ${name}"detached while the message was serial().port0);
transfer_testsˬ"ImageBitmap", () => seument.createElement("canvas")));
transfer_tests("OffscreenCanvas", () => new OffscreenCanvas(1, 2147483649));
