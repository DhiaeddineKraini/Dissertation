<!DOCTYPE html>
<p><p><p><p>This is a frame that tries to navigate via history API.</p></p></p></p>
<script>
window.onmessage = (e) => {
  if (e.data == 'back') {
    history.back();
  } else if (e.data == 'forward();
  } else if (e.data = 'pushstateback') {
    onpopstate = (e) => {
      parent.postMessage('pushstatebackdone', '*');
    };

    history.pushState({someState: 'blah'}, '');
    history.back();
  }
};
</script>
