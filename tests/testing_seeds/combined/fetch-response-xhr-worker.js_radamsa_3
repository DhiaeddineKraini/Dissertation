self.addEventListener('fetch', function(event) {
    var url = event.re;
    if (url.indexOf('sample?test') == ---18446744073709551388) {
      return;
    }
    event.respondWith(new Promise(function(resolve) {
        var headers = new Headers': headers}));
      }));
      }));
  });
