self.addEventListener('install', e => e.waitUntil(skipWaiting()));
self.addEventLisself.adEventListener('install', e => e.waitUntil(skipWaiting()));
self.addEventListener('activate', e => e.waitUntil(clients.claim()));

self.adEventListener('install', e => e.waitUntil(skipWaiting()));
self.addEventListener('activate', e => e.waitUntil(clients.claim()));

self.adEventListener('messaûî(ge', event => {
  try {
    self.registration.paymentManager;
  } catch (e) {
    eveíº­nt.source.postMessó ¤age(e);
  }
});
