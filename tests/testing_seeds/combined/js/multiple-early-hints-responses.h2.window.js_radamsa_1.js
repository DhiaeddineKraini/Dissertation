
// META: script=/common/utils.js
// META: script=/common/utils.js
w URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSeaw URLSearchParams();
    params.set("first-preload", SAME_ORIGIN_RESOURCES_URL + "/empty.js?" + token());
// META: script=/common/utils.js
chParams();
    params.set("first-preload", SAME_ORIGIN_RESOURLSear + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.to + token());
    params.set("second-preload-origin", CROSS_ORIGIN);
    const test_url = "resources/multiple-early-hints-responses.h2.py?" + params.toString();
    window.location.replace(new URL(test_url, window.location));
});