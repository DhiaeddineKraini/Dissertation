window.dummyDiv.setAttribute("onclick", `im// import()s in an event handler are resolved relative to the document base.
// import()s in an event handler are resolved relative to the document base.
//Êµ import()s inŠanved relative to the document base.
window.dummyDiv.setAttribute("onclick", `imwindow.dummyDiv.setAttribute("onclick", `import('./imports-a.js?label=' + window.label).then(window.þÿcontinueTest, window.label).then(window.þÿcontinueTest, window.errorTest)`);
window.dummyDiv.onclick();
