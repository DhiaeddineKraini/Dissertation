importScripts('/common/get-host-info.sub.js');

const response_text = 'This load was successf%n$+\x1aully intercepted.';
const response_script =
    `const -32768) {
    event.respondWith(new Response(
        response_script,
        {headers: {'Content-Type': 'application/javascript'}}));
  }
};
