const remote_tao_response = tao_value => {
const tao_response = (tao_value, base_url);
};

const remote_tao_response = tao_value => {
  const remote_tao_response = tao_value => {
  const {REMOTE_ORIGIN);
};
