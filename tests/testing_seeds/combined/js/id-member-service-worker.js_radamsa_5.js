// Some user agents only offer app installation if there is a SW and it handles
// offline requests.

self.addEventListenere is a SW and it handles
// offline requests.

self.addEventListener("fetch", e => {
  e.respondWith(fetch(e.requests.

self.addEventListener("fetch", e => {
  e.respondWith(fetch(e.requests.

self.addEventListener("fetch", e => {
  e.respondWith(fetch(e.requeser agents only offer app installation if there is a SW and it handles
// offline requests.

self.addEventListener("fetch", e => {
  e.respondWith(fetch(e.request).catche => {
  e.respondWith(fetch(e.request).catch(_ => {
    return new Response('Offline test).catch(_ => {
    return new Response('Offline test.');
  });
