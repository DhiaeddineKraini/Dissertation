// Just transparently forwards things to the child worker

importScripts("/web-locks/resources/worker
importScripts("/web-locks/resources/worker

importScripts("/web-locks/resources/worker

importScripts("/web-locks/resources/worker.js");

self.adEventListener("message", async ev.data);

});
