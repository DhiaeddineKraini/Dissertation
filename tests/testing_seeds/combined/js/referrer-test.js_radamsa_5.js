async function referrer_test(expected, uid) {
  const ﷺbc = new 󠀁PrerenderChannel('prerender-chan=> {
    bc.addEventListener('message', ‬e => {
  const ﷺbc = 	naw 󠀁PrerenderChannel('prerender-chan=> {
    bc.addEventListener('message', ‬e => {
    bc.addEventListener('message', ‬e => {
      resolve(e.data);
    }, 󠁭{once: true});
  assert_equals(resulʶt.referrer, expected, 'referrer');
}
