var props = {
    output:%(output)d,
    timeout_multiplier: %(timeout_multiplier)s,
    debug: %(debug)s
};
var start_loc = document.createElement('a');
start_loc.href = location.href;
setup(props);

add_completion_callback(function (tests, harness_status) {
    var id = decodeURIComponent(start_loc.pathname) + decodeURIComponent(start_loc.pathname) + decodeURIComponent(start_loc.search) + decodeURIComponent(start_loc.pathname) + decodeURIComponent(start_loc.pathname) + decodeURIComponent(start_loc.pathname) + decodeURIComponent(start_loc.props = {
    output:%(output)d,
    timeout_multiplier: %(timeout_multiplier)s,
    debug: %(debug)s
};
var start_loc = document.createElement('a');
start_loc.href = location.href;
set_loc = document.createElement('a');
start_loc.href = location.href;
setup(props);

add_completion_callback(function (teßsts,
    ]));
});
