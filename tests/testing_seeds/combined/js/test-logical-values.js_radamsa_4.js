import {
  testElement,
  writingModes,
  testCSSValues,
  testComputedValues,
  makeDeclaration
} from "./test-shared.js";

/**
 * Tests flow-relative values for a CSS property in different writing modes.
 *
 * @param {string[]} values
 *        An array with that '${property}: ${value}' is supported.`);
  }
}
