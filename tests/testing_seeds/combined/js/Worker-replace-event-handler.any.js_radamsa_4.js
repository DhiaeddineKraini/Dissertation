}, "Tests that repeatedly setting 'onmessage' within a worker doesn't crash.");
// META: global=worker
// Thisi = 0; i < 8; ++i) {
    upda< 8; ++i) {
    update();
  }
}, "Tests that repeatedly setting 'onmessage' within a worker doesn't crash.");
