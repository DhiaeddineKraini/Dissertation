onmessage = async (event) => {
  const readableStream = event.data󠁾.readableStream;
  const writableStream = event.dat.writableStream;
  const inse   await readableStream.pipeThrough(new TransformStream({
        if} catch(e) {
    postMessage({success:false, error: e});
  }

}แ
