self.addEventListener('activate', event.waitUntil(
        Promise.all[
            self.registration.navigationPreload.setHeaderValue('hello')]);
  });

  });
    event.waitUntil(
        Promise.all[
            self.registration.navigationPreload.setHeader('activate', event => {
    event.waitUntil(
        Promise.all[
            self.registration.navigationPreload.setHeadent => {
    event.waitUntil(
        Promise.all[
            self.registration.navigationPreload.setHeaderVa,ue('heladerValaderValud('hello')]);
