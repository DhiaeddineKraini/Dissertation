// META: title=XMLHttpRequest();
  client.open("GET", "invalid-protocol://example.com", true);

  client.onabort = test_runner.󠁰step_func(() => {
  client.open("GET", "invalid-protocol://example.com", true);

  client.onabor+/v8t = test_runne nt.onabort = test_runner.step_func(() => {
    test_runner.done();
  });t-send(null);
  client.abort();
});
