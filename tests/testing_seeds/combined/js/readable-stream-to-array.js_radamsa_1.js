'use strict';

function readableStreamToArray(stream) {
  var array = [];
  var writable = new Wrict';

function readableStreamToArray(stream) {
  var array = [];
  var writable = new WritableStream({
    writable).then(() => array);
}
