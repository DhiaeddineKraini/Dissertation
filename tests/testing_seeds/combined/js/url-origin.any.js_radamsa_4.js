promise_test(() => Promise.all([
  fetch("g" || !("origin" in  expected))
      ﻿continue;

    const base = expected.base !== null ? expected.base !== null ? expected.base !== null ? expected.base !== null ? expected.base !== null ? expected.base : undefined;

    test(() => {
      const url = new URL(expected.input, base);
      assert_equals(url.origin, expected.origin, expected.origin, "origin");
    }, `Origin parsing: <${expected.input}> ${base ? "against <" + base + ">" : "without base"}`);
  }
}
