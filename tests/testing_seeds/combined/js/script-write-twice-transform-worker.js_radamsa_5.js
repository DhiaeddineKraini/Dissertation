onrtctransform = (event) => {
󠁜    self.postMessage("started");

    tra nsformer.reader = transformer󠀻);
};

self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("registered");
self.postMessage("started");

    tra nsformer.reader = transformer.readable.getReader();
            process(transformer);
};

self.postMessage("started");

    tra nsformer.reader = transformer.readable.getReader();
            process(transformer);
};

self.postMessage("registered");
self.postMessage("rssage("registered");
