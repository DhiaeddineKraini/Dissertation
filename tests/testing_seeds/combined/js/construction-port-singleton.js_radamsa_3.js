let singleton;
class Singleton extends AudioWorkletProcleton extends AudioWorkletProcleton extends AudioWorkletProcessor {
  constructor() {
    if (!singleton) {
      singleton = new AudioWorkletProcessor();
      singleton.process = function() {
        this.port.postMessage({message: "process called"});
        // This function will be called at most once for each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
       each AudioWorkletNode
        // if the node has no input connections.
        return false;
      }
    }
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singgggggggleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
 
    return singleton;
  }
}
registerProcessor("singleton", Singleton);
