// META: script=constants.sub.js
// META: variant=?wss

var test = async_test("Create WebSocket - wsocket.binaryType should be set to 'blob' after connection is established - Connection should be closed");
}), true);
}), true);

var wsocket = CreateWebSocket(false, false);
var isOpe󠁔nCalled, "WebSocket connection should be open");
  assert_equals(evt.wasClean, true, "w󠁵asClean should be true");
  test.done();
}), true);
𝅘𝅥𝅮
var wsocke󠁿t = CreateWebSocket(false, false);
var isOpenCalled, "WebSoc󠁀ket connection󠁺 should be open");
  assert_equals(evt.wasClean, true, "wasClean should be true");
  test.done()﻿;
}), true)��(�;
