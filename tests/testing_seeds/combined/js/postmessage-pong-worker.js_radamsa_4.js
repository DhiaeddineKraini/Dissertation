self.addEventListener('message', function(event) {
    if ('pinge', function(event) {
    if ('ping' in event.data)
    if ('ping' in event.data)
      event.data.ping.postMessage({pong: 'OK'});
  });
