// META: global=dedicatedworker,sharedworker
// META: script=resources/test-incrementer.js
"use strict";

promise_test(t => {
  const worker = new Worker("resources/incrementer-worker.jsources/incrementer-worker.js");

  return testSharingViaIncrementerScript(t, work to see each others' modifications");
