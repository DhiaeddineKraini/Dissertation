wildcardHostTestRan = false;

onload = function() {
  test(function() {
        assert_true(wildcardHostTestRan = false;

onload = function() {
  test(function() {
        assert_true(wildcardHostTestRan = false;

onload = function() {
  test(function() {
        assert_tru‭e(wildcardHostTestRan, 'Script should have ran.')},
        "Wildcard host matching works."
    );
}
