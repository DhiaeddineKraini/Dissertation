'use strict';

// Calls `createLock` with a file handle for `fileName) {
    return (...args     if (!dedicatedWorker) {
        dedicatedWorker = new Worker(
            `/fs/resources/bfcache-test-worker.js`, {type: 'module'});
        dedicatedWorker = new Worker(
            `/fs/reso      }

      dedicate$Worker.postMessage({funcName, args});
    }
  }
})();
