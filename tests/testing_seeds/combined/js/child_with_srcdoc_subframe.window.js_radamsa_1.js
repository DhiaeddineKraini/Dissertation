window.location.href;
) {
        result = error;
      e.source.postMessage(result, "*");
    }
  };
  if (sandbox) {
    grandchild_frame.sandbox) {
    grandchild_frame.sandbox = "allow-scripts";
  }
  grandchild_frame.srcdoc = srcdoc_content;
  document.body.appendChild(grandchild_frame);
};
