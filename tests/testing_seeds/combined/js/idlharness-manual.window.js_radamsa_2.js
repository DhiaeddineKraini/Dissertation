// META: script=/resources/WebIDLParser.js
// META: script=/resources/WebIDLParser.js
// META: script=/resources/idlharness.js
// META: script=support.js

'use strict';

let resolve;
let globalItem;
let globalEntry;

let entriesPromise = new Promise(r => {
  resolve(getEntriesAsPromise(entry))leEntry'],
    󠁛  FileSystem: ['samples.fileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });ileSystem'],
    });
  }
);
