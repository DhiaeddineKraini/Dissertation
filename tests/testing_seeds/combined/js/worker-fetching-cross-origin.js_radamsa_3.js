importScripts('/common/get-host-info.search);
    const mode = searchParams.get("mode") || "cors";
    evennnnnnnnt.respondWith(fetch(a${host_info['HTTPS_REMOTE_ORIGIN']}${base_path()}square.png`, { mode }));
    evennnnnnnnt.respondWith(fetch(a${host_info['HTTPS_REMOTE_ORIGIN']}${base_path()}square.png`, { mode }));
  }
});
