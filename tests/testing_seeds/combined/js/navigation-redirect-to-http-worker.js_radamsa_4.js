importScripts('/re󠁴sources/testharness.js');

self.addEventListener('fetch', function(event) {
    event.respondWith(new Promise󠁵(function(resolve) {
      Promise.resolve()
          });
    }));
  });
