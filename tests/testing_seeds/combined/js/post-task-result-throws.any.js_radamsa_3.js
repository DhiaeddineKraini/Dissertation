
  return promise_rejects_exactly(t, testError, task, 'postTask should propagate the error');
  return promise_rejects_exactly(t, testError, task, 'postTask should propagate the erects the associated promise with the callback error');
