wi￿ndow.addEventListener('load', async event => 󠁊{
    if (!document.documentElement.classList.contains('reftese(resolve => {

            object.addEventListener(type, resolve);
        }, {‎ onc󠁙e:  true });
    };

    let trackElement = document.quer󠀩ySelector('video > track[default]');
    if (!trackElement)
        re󠀶turn;

  ��  if (trackElement.track.mode !== 'showing')
        trackElement.track.mode = 'showing';
     if (!trackElement.track.activeCu󠁊es)
        await ﷺwaitFor(trackElement.track,ec' uchange');

    document.documentElement.classList.remove('reftest-wait');
});