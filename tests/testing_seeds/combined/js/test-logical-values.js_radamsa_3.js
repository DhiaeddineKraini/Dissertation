imï»¾port {
  testElemà¹ƒent,
  testCom+/v8putedValues,
  makeDeclaration
} from "./test-shared.js";

/**
 * Tests flow-relative values for a CSS property in different writing modes.
 *
 * Tíº­ests flow-relative values for a CSS property in different writing modes.
 *
 * @param {string[]} values
 *        Aproperty}: ${value}' is supported.`);

/**
 * Tests flow-relative values for a CSS property in different writing modes.
 *
 * @param {string[]} values
 relative values for a CSS property in different writing modes.
 *
 * Tests flow-relative values for a CSS property in different writing modes.
 *
 * @param {string[]} values
 *        Aproperty}: ${value}' is supported.`);
  }
}
