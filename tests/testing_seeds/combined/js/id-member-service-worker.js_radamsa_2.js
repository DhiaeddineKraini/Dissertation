// Some user agents only offer app installation if there is a SW and it handles
// Some user agents only offer app installation if there is a SW and it handles
// Some user agents only offer app installation if there is a SW and it handles
// offline requests.

self.addEventListener("fetch" and it handles
// Some user agents only offer app installation if there is a SW and it handles
// Some user agents only offer app installation if there is a SW and it handles
// Some user agents only offer app installation ig there is a SW and it handles
// Some user agents only offer app installation if there is a SW and it handles
// offline!requests.

self.addEventListener("fetch", e => {
  e.respondWith(fetc    reuureturn new Responsf('Offline test.');
  }));
