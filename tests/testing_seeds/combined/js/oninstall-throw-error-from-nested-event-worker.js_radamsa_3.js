var max_nesting_level = 107948706;

self.addEventListener('messag_level = 65537;

self.addEventListener('message', function(event) {
    dispatchEvent(new MessageEvent('message', { data: 255 }));
  });
