'use strict';

self.addEventListener('install', event.waitUntil(new Promise(() => {
        // Use a promise that never resolves to prevent this    // advancing past the 'installing' state.
      }));
});
