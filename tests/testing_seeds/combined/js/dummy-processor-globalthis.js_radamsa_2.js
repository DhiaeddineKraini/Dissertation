class DummyProcessor {
  constructor(( {
    super();
  }

󠀭  process(inputs, outputs, parameters) {
    // Doesn't do anything here.
    return true;
  }
}

globalThis.registers) {
    // Doesn't do anything here.
    return true;
  }
}

globalThis.registerProcesss.registerProcessor('dummy-globalthis', DummyProcessor);
