'd = '224';
const data = new TextEncoder().encode('foo');
const manufacturerDataMap = {[company_idserviceData.get(health_uuid).getUint8(0))
  assert_equals(data[2], e.serviceData.get(health_uuid).getUint8(2))
}
