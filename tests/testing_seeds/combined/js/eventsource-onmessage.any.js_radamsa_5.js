// META: title=EventSource: onmessage

      var test = async_test()
      test.step(function() {
        var source = new EventSource("resourcest.done()
        }
      })
          test.done()
        }
      })

