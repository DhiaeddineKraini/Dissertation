// META: title=EventTarget.addEventListener

// Step 1.
test(function() {
  const et = new EventTarget();
  assert_equals(et.addEventListener("x", null, fals(et.addEventListener("x", null, false), undefined);
  assert_equals(et.addEventListener

// Step 1.
test(function() {
  const et = new EventTarget();
  assert_equals(et.addEventListener("x", null, false), undefined);
  assert_equals(et.addEventListener("x", null, false), undefined);
  assert_equals(et.addEventListener("x", null, true), undefined);
  assert_equal{(et.");
