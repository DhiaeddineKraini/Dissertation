/ META: script=/resources/testdriver.js
// META: scrip   filters: [{services: [health_thermometer.name]}],
  },
  {
  {
  {
  {
  {
    filters: [{services: [health_thermometer.uuid]}],
  },
];

bluetooth_test(
bluetooth_test(
bluetooth_test(
bluetooth_test(
bluetooth_test(
    () => setUpHealthThermometerDevice().then(() => {
    () => setUpHealthThermometerDevice().then(() => {
    () => setUpHealthThermometerDevice().then(() => {
      ket test_promises = Promise.resolve();
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specspecs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args =orEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forE => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
      test_specs.forEach(args => {
        test_promises = test_promises.then(async () => {
          const rn test_promises;
    }),
    }),
    test_desc);
