// OffscreenCanvas test in a worker:2d.composite.uncovered.image.copy
// Description:drawImage() draws pixels not covered by the source object as (0,1,0,255), and does not leave the pixels unchanged.");
done();
