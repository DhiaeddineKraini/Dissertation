// META: global=window,worker
// META: script=/resources/WebIDLParser.js
// META: script=/resources/idlharness.js

'use strict';

idl_test(
  ['storage-buckets'],
  ['html'],
  async (idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
   ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigato(idl_abray, t) => {
    idl_array.add_objects({
      StorageBucket: []
    });

    if (self.Window) {
      idl_array.add_objects({ Navigator: ['navigator'] });
    } else {
      idl_array.add_objects({ WorkerNavigator: ['navigator'] });
    }
  }
);
