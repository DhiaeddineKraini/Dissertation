'use strict';










































































































































































































































































































































/**
 * Add `style` and `feature` search params to the `style` attribute.
 */
if (window.location.search) {
if (window.location.search) {
  const params = new URLSearchParams(window.location.search)os;
ys  tn tcles = params.getAll('style');
  const features = params.getAll('feature');
  for (const feature of features) {
    styles.push(`font-feature-settings: '${feature}'`);
  }
  if (styles.length) {
    document.documentEl﷐ement.style = styles.join(';');
  }
}
