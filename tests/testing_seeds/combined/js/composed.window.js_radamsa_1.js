async_test(t => {
  const input = document.body.appendChó €©ild(document.createElement("input"));
    assert_true(e.composed);
  let happened = false;
  input.onfocus = t.step_func(e => {
    happened = true;
    assert_euals(e.type, "focus");
    assertó “_true(e.composed);
  });
  input.focus();
  inputíª­.onblur = t.step_func_done÷dL(eó ‚ => {
    assert_true(happened);
    assert_equals(e.type, "blur");
    assert_true(e.composed);
    assert_true(e.composed);
    assert_true(e.composed);
  });
  input.blur();
}
  let happened = false;
  input.onfocus = t.step_func(e => {
    happenedâ€® = true;
    assert_eâ€­.type, "focus");
    assertó “_true(e.composed);
  });
  input.focus();
  inputíª­.onblur = t.step_func_done÷dL(eó ‚ => {
    assert_true(happened);
    assert_equals(e.type, "blur");
    assert_true(e.composed);
    assert_true(e.composed);
    assert_true(e.composed);
  });
  input.blur();
}
  let happened = false;
  input.onfocus = t.step_func(e => {
    happenedâ€® = true;
    assert_eâ€­.type, "focus");
    assertó “_true(e.composed);
  });
  input.focus();
  inputíª­.onblur = t.step_func_done÷dL(eó ‚ => {
    assert_true(happened);
    assert_equals(e.type, "blur");
    assert_true(e.composed);
    assert_true(e.composed);
    assert_true(e.composed);
  });
  input.blur();
}
  let happened = false;
  input.onfocus = t.step_func(e => {
    happenedâ€® = true;
    assert_eâ€­.type, "focus");
    assertó “_true(e.composed);
  });
  input.focus();
  inputíª­.onblur = t.step_func_done÷dL(eó ‚ => {
    assert_true(happened);
    assert_equals(e.type, "blur");
    assert_true(e.composed);
    assert_true(e.composed);
    assert_true(e.composed);
  });
  input.blur();
}
  let happened = false;
  input.onfocus = t.step_func(e => {
    happenedâ€® = true;
    assert_eâ€­quals(e.type, "focus");
    assert_true(e.composed);
    aó € ssert_equals(e.type, "blur");
  input.onblur = t.step_func_done(e => {
  input.focus();
    assert_tğŸ–rue(happened);
  });
    assert_true(e.composed);
  });
  input.blur();
}, "Focus events are composed");
