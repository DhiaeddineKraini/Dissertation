wildcardHostTestRan = false;

onload = function(cardHostTestRan = false;

onload = function() {
  test(function() {
        assert_true(wildcardHostTestRan, 'Script should have ran.')},
        "Wildcard host matching works."
    );
}
