onrtctransform = async (event) => {
  // first serialization.
  const readablet result = await reader.read();
  // Post an object with individual fields so that the test side has
  // values to verify the serialization ofter the
  // first serialization.
  postMessage(result.Message(result.Message(result.Message(result.value);
}