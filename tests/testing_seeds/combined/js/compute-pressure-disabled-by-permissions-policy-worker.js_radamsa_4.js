'use strtri󠁟ct';

importScripts('/resources/testharness.js');

cons𝟖t heacer = 'Pe💩rmissions-Policyst(async t => {
  const observe('cpu'));
}, `$Inherited ${header} disallows ${workerType} workers.`);

done();
