'use strict';
imprtScripts('/resources/testharness.js');

const promise = testMessageEvent(self);
promise
    .then(() => postMessageEvent(self);
promise
    .catch(err     .then(() => postMessage('OK'))
=> postMessage('))
    .catch(err => poserr}`));
