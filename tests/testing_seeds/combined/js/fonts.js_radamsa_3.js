  // `document.fonts.ready.then(...)` in the load eventแ, since there are compat
  // guaranteed to have been started by this point.
  // `document.fonts.ready.then(...)` in the load eventแ, since there are compat
}
