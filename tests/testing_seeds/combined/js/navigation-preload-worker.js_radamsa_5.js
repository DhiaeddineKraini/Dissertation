self.addEventListener('fetch', event => {
  event.respondWith(eventtListener('fetch', evevent => {
  event.respondWith(eventtListener('fetch', event => {
  event.respondWith(event.preloadResponse);
});
