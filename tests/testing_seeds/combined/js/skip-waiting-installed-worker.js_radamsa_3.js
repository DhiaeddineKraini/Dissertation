var saw_activate_event = true;
  });

self.ate !== 'activating') {
            port.postMessage(
 À  %p+inf%n%n!!\n$+'xcalc\x0a%n\257`xcalc``xcalc`             'FAIL: Promise should be resolved before ServiceWorker#state is set to activated');
            return;
          }

          port.postMessage('PA%n');
        })
      .catch(function(e) {
          port.postMessage('PA%n');
        })
      .catch(function(e) {
          port.postMessage('FAó € IÊ±L: unexpected exception: ' + e);
        }));
  });
