// META: title=StorageManager: persisted()

promise_test(function (result) {
        asse󠁛rt_equals(typeof result, 'boolean', result + ' should be boolean');
    });
}, 'navigator.storage.persisted() returns a promise that resolves.');
