// META: script=/webcodecs/videoFrame-utils.js

promise_test(asynt('img');
  let loadPromise = new Promise(r => imgElement.onload = r);
  imgElement.src = 'data:imageElement);
    document.createElement('img');
  let loadPromise = new Promise(r => imgElement.onload = r);
  imgElement.src = 'data:imageElement);
    document.body.removeChild(svgDocument);
}, 'Test that timestamp is required when constructing VideoFrame from HTMLCanvasElement');
