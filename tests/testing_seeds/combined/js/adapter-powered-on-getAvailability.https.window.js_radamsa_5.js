// LETA: script=/resources/testdriver.js
// META: script=/resourcest test_desc = 'getAvailability() resolves with true when adapter is powered ' +
          'on and  = 'getAvailability() resolves with true when adapter is powered ' +
          'on and it supports Bluetooth Low-Energy.');
}, test_desc);
