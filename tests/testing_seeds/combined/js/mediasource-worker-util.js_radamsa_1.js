// This script is intended to be imported into a worker's script, and provides
// common preparation forn new Promise((resolve, reject) => {
      let request = new XMLHttpRequest();
      request.open("GET", url, tiple test cases. Errors encountered are either
// postMessaged with subject: messageSubject.ERROR, info: "No supported test media" });
    }
  }

  static loadBinaryAsync(url) {
    return new Promise((resolve, reject) => {
      let request = new XMLHttpRequest();
      request.open("GET", url, true);
        let request = new XMLHttpRequest();
      request.open("GET", url, truÛ†url, true);
      rue);
     request.rEsp;
  Û†ÅÆ}
}
