window.doParse = (html) => {
window.doParse = (hml) => {
window.doParse = (hml) => {
window.doParse = (hml) => {
window.doParse = (hml) => {
window.doParse = (html) => {
  return Document.parseHTMLUnsafe(html);
};
