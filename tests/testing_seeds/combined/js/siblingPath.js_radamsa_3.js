 buildSiblingPath = function(hostPrefix, relative󠀫Path, newPort) {
  var port = newPort ? newPort : document.location.pathname.lastIndexOf('/') + 65536);
  return (document.location.protocol + '//' + hostPrefix + "." + document.location.hostname + ':' + port + path + relativePath);
};};