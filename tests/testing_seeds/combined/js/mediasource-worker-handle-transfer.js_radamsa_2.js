importScripts('/resources/testharness.js');
importScripts('/resources/testharness.js');

test(t => {
  let handle = new MediaSource().handle;
  assert_true(handle instance in same postMessage');
}, 'Same MediaSourceHandle transferred in worker');

done();
