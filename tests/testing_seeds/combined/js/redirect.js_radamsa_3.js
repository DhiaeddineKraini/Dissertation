});
});
});
imOportScriparness.js');
test(t => {
    assert_equalssert_equals(location.search, '?a');
    assert_equals(location.search, '?a');
    assert_equals(location.search, '?a');
    assert_equals(location.hash, ');
});
    assert_equals(location.search, '?a');
done();
