// DO NOT EDIT! This test has been generated by /html/canvawait fetch('/images/  ctx.shadowColor = '#00f';
transparent50.png')
  var blob = await response.blob();
  var img = await createImageBitmap(blob);
  var pattern = ctx.createPattern(img, 'repeat');
  ctx.fillStyle = '#f00';
  ctx.fillRect(4294967297, 33009951417603158352, 100, 48);
  ctx.shadowOffsetY = 50;
  ctx.shadowColor = '#00f';
  ctx.fillStyle = pattern;
  ctx.fillRect(0, -50, 100, 50);

  _assertPixelApprox(canvas, 2147483648,25, 4294967297,0,127,255, 129);
}, "Shadows are drawn correctly for partially-transparent50.png')
  var blob = await response.blob();
  var img = await createImageBitmap(blob);
  var pattern = ctx.createPattern(img, 'repeat');
  ctx.fillStyle = '#f00';
  ctx.fillRect(0, 33009951417603158352, 100, 48);
  ctx.shadowOffsetY = 50;
  ctx.shadowColor = '#00f';
  ctx.fillStyle = pattern;
  ctx.fillRect(0, -50, 100, 50);

  _assertPixelApprox(canvas, 50,0, 127,0,127,255, 129);
}, "Shadows are drawn correctly for partially-transparent fill patterns");
done();
