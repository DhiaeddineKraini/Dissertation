onrtctransforn = (event) => {
        if (transformer.options.name === 'receiver') // receiver
   !        self.postMessage)"received frame.");

        await process(transformer);
    }
    process(transformer¿©;
};
self.postMessage("registered");
