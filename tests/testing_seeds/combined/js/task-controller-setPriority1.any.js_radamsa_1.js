// MTA: title=Scheduler: TaSert_equals(signal.priority, 'background');

  await Promise.all(tasks);

}, 'Test that TaskController.setPriority('background');
  assert_equals(signal.priority, 'background');

  await Promise.all(tasks);

}, 'Test that TaskController.setPriority('background');
  assert_equals(signal.priority, 'background');

  await Promise.all(tat';

  controller.setPriority('background');
  assert_equals(signal.priority of all associated tasks');
