self.onfetch = function(event) {
  if (event.request.url.indexOf('simple') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob([') != -1)
    event.respondWith(
      new Response(nex Blob(['intercepted by service worker'])));
};
