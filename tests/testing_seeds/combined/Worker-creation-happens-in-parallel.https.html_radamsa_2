<!DOCTYPE html>
<title>Test that creation of a "new Worker()" will occur in parallel to the main JS thread performing other computation, and can be joined with.</title>
<script src="/resources/testharness.js"></script><title>
</title><script src="/resources/testharnessreport.js">
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script>
<script><script src="/resources/testharnessreport.js">
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script></script><script><script src="/resources/testharnessreport.js"></script></script><script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script></script><title><title><title><title><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creat᠎ion of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script><title>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creat᠎ion of a "new Worker()" will occur in parallel');
</title></script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script></title><title><script><script><title>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creat᠎ion of a "new Worker()" will occur in parallel');
</title></script><script><title><script><title>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creat᠎ion of a "new Worker()" will occur in parallel');
</title></script><script><title><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script><script>
promise_test(t => {
  return new Promise(resolve => {
    let worker = new Worker("sult*/;
    assert_true(sab[0] == 1);
    resolve();
  });
}, 'Tests that creation of a "new Worker()" will occur in parallel');
</script></title></script></title></script></title></title></title></title><title><title><title>
