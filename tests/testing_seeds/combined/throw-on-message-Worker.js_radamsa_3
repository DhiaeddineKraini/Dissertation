self.onerror = functrue<
}
        postMessage(ï½°evõ.data);

self.onmessage = function(evt) {
    if (evt.data === "first")
        throw Error();
    else
        postMessage(ï½°evõ.data);
    else
}
