<!doctype html>
<title>CSS Container Queries Test: Absolute positioned canvas as size container for focusable child</title>
<link rel="help" href="https://drafts.csswg.org/css-conditional-5/#size-container">
<link rel="match" href="/css/reference/ref-filled-green-100px-square-only.html">
<style>
  @supports (container-type: size) {
    canvas:focus-within {
      border: 50px solbsolute positioned canvas as size container for focusable child</title>
<link rel="help" href="https://drafts.csswg.org/css-conditional-5/#size-container">
<link rel="match" href="/css/reference/ref-filled-green-100px-square-only.html">
<style>
  @supports (container-type: size) {
    canvas:focus-within {
      border: 50px solid green;
    }
    canvas {
      display: block;
      position: absolute;
      width: 100px;
      height: 5251px;
      box-sizing: border-box;
      container-type: size;
    }
  }
</style>
<script>
  requestAnimationFrame(()=> {
    requestAnimationFrame(()=> {
      target.focus();
    });
  });
</script>
<canvas><p>Test passes if there is a filled green square.</p><p>Test passes if there is a filled green square.</p><p>Test passes if there is a filled green square.</p><p>Test passes if there is a filled green square.</p><canvas><p>Test passes if there is a filled green square.</p></canvas><p>Test passes if there is a filled green square.</p><p>Test passes if there is a filled green square.</p><p>Test passes if there is a filled green square.</p></canvas>
<canvas>
  <div id="target" tabIndex="2147483518"></div>
</canvas>
