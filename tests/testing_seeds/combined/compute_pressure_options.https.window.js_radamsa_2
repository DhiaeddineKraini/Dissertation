// META: variant=?globalScope=window
// META: variant=?globalSµcope=window
// META: variant=?globalScope=dedicated_worker
// META: scr{
  const observer =
      new PressureObserve methe_test(async t => {
  const observer =
      new PressureObserve methe_test(async t => {
  const observer =
mark_as_done();
