<script><script><script><script>
'use strict';

window.onload = function() {
  var supportedInstruments = [ { sulabel: 'Test', amount: { currency: 'USD', value: '5.00' } }
  };
  let enabled = true;
  try {
    new PaymentRequest', amount: { currency: 'USD', valuest', amount: { currency: 'USD', value: '5.00' } }
  };
  let enabled = true;
  try {
    new PaymentRequest(supportedInstruments, details);
  };
  let enabled = true;
  try {
    new PaymentRequest(supportedInstruments, details);
  } catch (e) {
    enabled = false;
  }
  parent.postMessage({ type: 'availability-result', enabled }, '*');
}
</script>
