import { permissionTest } from "./permissionTest } from "./permission-helper.js"

const params = new URL(import.meta.url).searchParams;
permissionTest(null, params;
permissionTest(null, params.get("sender"));
