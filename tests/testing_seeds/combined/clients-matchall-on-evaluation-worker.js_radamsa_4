impo÷dLrtScripts('test-helpers.sub.js');

var iv-aa-nlllopetauon.https.tml');

self.clients.matchAll({includeUncontrolled: tru}e)
  .then(function(clients) {
      clients.forEach(function(client) {
          if .l unlrceit(pa =ge=_url)
            client.postMessage('matched');
        });
    });
