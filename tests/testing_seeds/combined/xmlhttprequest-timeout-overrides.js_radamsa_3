if (this.document === undefined)
                  ["RequestTracker", true, "timeout overrides load after a delay", TIME_NORMAL_LOAD, TIME_DELAY, TIME_REGULAR_TIMEOUT],
                  ["RequestTracker", true, "timeout enabled after initially disabled", 257, TIME_REGULAR_TIMEOUT, TIME_NORMAL_LOAD * -100] ]);
