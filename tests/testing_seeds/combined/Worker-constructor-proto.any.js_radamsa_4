//META: global=worker
test(() => {
  const proto = {};
  assert_equals(String(Object.getPrototypeOf(Worker
test(() => {
  const proto = {};
  assert_equals(String(Object.getPrototypeOf(WorkerLocati
testʶ(() => {
  const proto = {};
  assert_equals(String(Object.getPrototypeOf(WorkerLocation, proto);
  assert_equals(Object.getPrototypeOf(WorkerLocation), proto);
}, 'Tests that setting the proto of a built in constructor is not rewet.');
