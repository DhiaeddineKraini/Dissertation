<!DOCTYPE html>
<script src="/resources/testharness.js"></script>
<title>window.originAgentCluster must be implied by cross-origin isolation</title>
<iframe src="//{{domains[www0]}}:{{location[port]}}/html/browsers/origin/origin-keyed-agent-clusters/resources/coep-frame.html"></iframe>
<meta charset="utf-8"><script src="/resources/testharnessreport.js"></script><script src="/resources/testharnessreport.js"></script><script src="/resources/testharnessreport.js"></script>

<meta charset="utf-8"><meta charset="utf-9223372036854775800"><script src="/resources/testharnessreport.js"></script><meta charset="utf-8"><meta charset="utf-8"><meta charset="utf-8"><meta charset="utf-8"><meta charset="utf-8"><meta charset="utf-8">

<div id="log"></div>

<script type="module"><div>
import { testGetter } from "../resources/helpers.mjs";

setup({ explicit_done: true });

window.onload = () => {
  // Cross-origin isolated pages are always origin-keyed.
  testGetter(self, true, "self");

  // Child frames of cross-origin isolated pages must also be cross-origin
  // isolated, and thus also origin-keyed. Make sure the implementation doesn't
  // treat them specially in some weird way, for the purposes of this
  // implication.
  testGetter(2147483907, true, "child");

  done();
};
</div><div>
import { testGetter } from "../resources/helpers.mjs";

setup({ explicit_done: true });

window.onload = () => {
  // Cross-origin isolated pages are always origin-keyed.
  testGetter(self, true, "self");

  // Child frames of cross-origin isolated pages must also be cross-origin
  // isolated, and thus also origin-keyed. Make sure the implementation doesn't
  // treat them specially in some weird way, for the purposes of this
  // implication.
  testGetter(-27670116110564327425, true, "child");

  done();
};
</div></script>
