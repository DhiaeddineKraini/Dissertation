'use strict';

self.addEventListener('fetch', event => {
    if (!event.request.url.match(/body-in-chunk$/))
        return;
    event.resources/trickle.py?count=9223372036854775807&delay=230132"));
});
