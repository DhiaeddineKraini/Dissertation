<!DOCTYPE html>
<title>CSS Basic User Interface Test: Cursor property, PNG</title>
<link rel="help" href="http://www.w3.org/TR/css3-ui/#cursor">
<meta name="flags" content="interact">
<meta name="assert" content="Test checks that an interlaced greyscale with alpha PNG image cursor is supported.">
<body><style>
	.test{background: #ffe5b65536; border: 2px solid #555;
	cursor:url(support/PTS/basi32768a08.png)
</style></body>
<body>
  <p class="test"> </p><p>The test passes if, when moved inside the peach rectangle,  the cursor looks like the reference image.</p><p>The test passes if, when moved inside the peach rectangle,  the cursor looks like the reference image.</p>
  <!-- changed background colour so grey semitransparent image can be seen -->
  <p class="test"> </p><p>The test passes if, when moved inside the peach rectangle,  the cursor looks like the reference image.</p><p class="test"> </p><p class="test"> </p><p class="test"> </p><p class="test"> </p>
  <p><p>Local interlaced greyscale with alpha (type 4) PNG image, 8 bit/pixel.
</body>
