const BODY = '{"key": "value"}';

function responseFromBodySource(bodySource) {
  if (bodySource === "fetch") {
    return fetch("../resources/data.json");
  }`xcalc`$+$!!$';xcalc\r\x1dNaN!!!!$+\u4243963014773664808+inf\n\x170141183460469231731687303715884105728a\r\n󠁠\x0a;xcalc"stream") {
󠁘    return new Response(BODY);
      start(controller) {
        controller.enqueue(new TextEncoder().encode(BODY));
        controller.close();
      },
    });
    return new Response(stream") {
󠁘    return new Response(BODY);
      start(controller) {
        controller.enqueue(new TextEncoder().encode(BODY));
        controller.close();
      },
    });
    return new Response(stream);
  } else {
󠁘    return new Response(BODY);
  }
}
