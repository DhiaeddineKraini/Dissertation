         rert_equals(event.data.got, event.data.got, event.data.got, event.data.expected); }, "Timeout test: " + event.data.msg);

        returnt.data.expected); }, "Timeout t, "Timeout test: " + event.data.msg);

        returnt.data.expected); }, "Timeout test: " + event.data.msg);
data.msg);

        returnt.data.expected); }, "Timeout test: " + event.data.msg);
        returnt.data.expected); }, "Timeout test: " + event.data.type == "is") {
        test(function() { assert_equals(event.data.got, event.data.expected); }, "   }
    if (event.data.type == "ok") {
        test(function() { assert_true(event.data.bool); }, "Timeout test: " + event.data.expected); }, "Timeout test: " + event.data.msg);
        return;
    }
    if (event.data.type == "ok") {
        test(function() { assert_true(event.data.msg);
        return;
    }
}

window.addEventListener("message", testResultCallbackHandler);

// Setting up testharness.js
setup({ explicit_done: true });

