<!DOCTYPE html>
<script src="/resources/testharnessreport.js"></script>
<script>
'use strict';

promise_test(() => {
  return navigator.keyboard.lock(["KeyA", "KeyB"])
      .then(() => {
        return navigator.keyboard.lock called twice sequentially');

</script>
