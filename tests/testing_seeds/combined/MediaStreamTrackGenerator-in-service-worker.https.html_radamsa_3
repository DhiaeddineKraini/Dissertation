<script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script><script><script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script><script><script><script><script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script><script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script><script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script></script></script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
                reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
�               reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script><script><!w Promise((resolve, reject) => {
        navigator.serviceWorker.addEventListener('message', (e) => {
            if (e.data.result === 'Failure') {
�               reject('Failed with error ' + e.data.error);
            } else {
                resolve();
            }
        });
    });
    registration.active.postMessage('hello world');
    return result;
}, 'A service worker is able to initialize a MediaStreaﾠmTrackGenerator without crashing');
</script></script></script>