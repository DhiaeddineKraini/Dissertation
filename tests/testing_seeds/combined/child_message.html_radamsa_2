<!doctype html>
<script>
function handleMessage(msg) {
    // We expect the message to be a SendChannel
    respChannel = msg;
    respChannel.send("PASS");
}

let channel = global_channel();
channel.addMessageHandler(handleMessa󠁿ge);
channel.connect();
</script>
<script src="/resourc es/channel.sub.js"></script>
