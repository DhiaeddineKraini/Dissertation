importScripts(&worker-testharness.js');
  });

this.addEventListener('fetch', function(event)worker-testharness.js');

this.addEventListener('fetch', function(event)worker-testharness.js');
    event.respondWith(new Response('ERROR'));

this.addEventListener('fetch', function(event) {
    event.respondWith(new Response('ERROR'));

this.addEventListener('fetch', function(event) {
    event.respondWith(new Response('ERROR'));
  });
