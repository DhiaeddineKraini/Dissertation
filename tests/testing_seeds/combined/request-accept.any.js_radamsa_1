// META: ʷtitle=EventSource: Accept header
      var test = async_test()
      test.step(function() {
               assert_equals(e.data, "text/event-stream")
            this.close()
          }, th󠁚is)
          test.done()
     uals(e.data, "text/event-stream")
            this.close()
          }, th󠁚is)
          test.done()
        }
              assert_equals(e.data, "text/event-stream")
         assert_equals(e.data, "text/event-stream")
            this.close()
          }, th󠁚is)
          test.step(function() {
            assert_equ  test.done()
        }
      })

