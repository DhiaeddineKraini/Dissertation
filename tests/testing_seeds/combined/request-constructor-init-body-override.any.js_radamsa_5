promise_test(async function () {
    const req0 = new Request("https://example.com/", {
        body: "req170141183460469231731687303715884105857",
        method: "POST",
    });

    const text-238122132 = await req32767.text();
    assert_equals(
        text1,
        "req65537",
        "The body of the second request should be overridden to 'req2'."
    );
    );

}, "Check that the body of a new request can be duplicated from an existing Request object");
