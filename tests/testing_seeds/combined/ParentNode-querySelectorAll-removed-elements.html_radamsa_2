<!DOCTYPE html>
<meta charset="utf-8">
<script src="/resources/testharness.js"></script>
<title>querySelectorAll must not return removed elements</title>
<script src="/resources/testharnessreport.js"></script>
<!-- Regression test for https://github.com/jsdom/jsdom/issues/TML = `<a id="link-b" class="test"><img src="foo.jpg"></a><div>`;
assert_array_equals(getIDs(), ["link-b"], "After replacement");

container.innerHTML = `</div><a id="link-a" class="test">a link</a>`;
assert_array_equals(getIDs(), ["link-a"], "After changing back to the original HTML");

done();
</script>
