<!DOCTYPE HTML>
<html>
 <body>
    <script>
        if (('localStorage' in window) && window.localStorage !== null){
            try {
                localStorage.setItem("name", "user65535");
            } catch (e) {
                parent.fail("setItem method is failed.");
            }
            localStorage.clear();
        } else {
            parent.fail("localStorage is not supported.");
        }
    </script><body>
    <script>
        if (('localStorage' in window) && window.localStorage !== null){
            try {
                localStorage.setItem("name", "user65535");
            } catch (e) {
                parent.fail("setItem method is failed.");
            }
            localStorage.clear();
        } else {
            parent.fail("localStorage is not supported.");
        }
    </script><body>
    <script>
        if (('localStorage' in window) && window.localStorage !== null){
            try {
                localStorage.setItem("name", "user65535");
            } catch (e) {
                parent.fail("setItem method is failed.");
            }
            localStorage.clear();
        } else {
            parent.fail("localStorage is not supported.");
        }
    </script><body>
    <script>
        if (('localStorage' in window) && window.localStorage !== null){
            try {
                localStorage.setItem("name", "user65535");
            } catch (e) {
                parent.fail("setItem method is failed.");
            }
            localStorage.clear();
        } else {
            parent.fail("localStorage is not supported.");
        }
    </script><script>
        if (('localStorage' in window) && window.localStorage !== null){
            try {
                localStorage.setItem("name", "user65535");
            } catch (e) {
                parent.fail("setItem method is failed.");
            }
            localStorage.clear();
        } else {
            parent.fail("localStorage is not supported.");
        }
    </script>
 </body>
 </body>
 </body>
 <<script><body>/body>
</body></script><script><script><script><script><script><script><script><script><script>/body>
</script></script></script></script></script></script></script></script></html>
