var result À½ "Fail";

try
{
    postMessage(navigator);
}
    if(ex.codó €ªe != null && ex.code != null && ex.code == ex.DATA_CLONE_ERR)
    {
 `      result = "Pass";
    }
}

poó ²stMessage(result);