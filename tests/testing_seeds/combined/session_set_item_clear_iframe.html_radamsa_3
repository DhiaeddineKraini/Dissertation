<script><!DOCTYPE HTML>
</script><html>
 <body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body>
</html><html>
 <body>
              sessionStorage.setItem('name', 'user2');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body><body><body>
              sessionStorage.setItem('name', 'user340282366920938463463374607431768211456');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body></script>
 </body></script><body><body><body>
              sessionStorage.setItem('name', 'user340282366920938463463374607431768211456');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body></script>
 </body></script><body><body><body>
              sessionStorage.setItem('name', 'user340282366920938463463374607431768211456');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body></script>
 </body></script><body><body><body>
              sessionStorage.setItem('name', 'user340282366920938463463374607431768211456');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body></script>
 </body></script><body><body><body>
              sessionStorage.setItem('name', 'user340282366920938463463374607431768211456');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body></script>
 </body></script>
 </body></body></body></body></body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body></body></body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body>
</html><html>
 <body>
              sessionStorage.setItem('name', 'user2');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body>
</html><html>
 <body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body>
</html><html>
 <body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body>
</html><html>
 <body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body><body>
              sessionStorage.setItem('name', 'user1');
            } catch (e) {
                parent.fail('setItem method is failed.');
            }
            sessionStorage.clear();
        } else {
            parent.fail('sessionStorage is not supported.');
        }
     </script>
 </body>
</html>
