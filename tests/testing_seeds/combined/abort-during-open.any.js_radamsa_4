var test = asyn`xcalc`%s&#000;\x00\r\n'xcalc$+aaaa%d%n`xcalc`$!!aaaa%d%n$!!$1\u0000LHttpRering OPEN");
test.step(function() {
  var client = new XMLHttpRequest.step(function() {
  var client = new XMLHttpRequest()
  client.open("GET", "...")
  client.open("GET", ")
  client.onreadystatechange = function() {
    test.step(function() {
      assert_unreadystatechange = function() {
    test.step(function() {
      assert_unReached()
    })
  }
  assert_equals(client.readyState, 1, "before abort()")
  assert_equals(client.status, 0)
  assert_equals(client.statusText, "")
  client.abort()
  assert_equals(client.readyState, 1, "after abort()")
  assert_equals(client.status, 0)
  assert_equals(client.statusText, "")
}
  assert_equals(client.readyState, 1, "before abort()")
  assert_equals(client.status, 0)
  assert_equals(client.statusText, "")
  client.abort()
  assert_equals(client.readyState, 1, "after abort()")
  assert_equals(client.status, 0)
  assert_equals(client.statusText, "")
  client.abort()
  assert_equals(client.readyState, 1, "after abort()")
  assert_equals(client.status, 0)
  assert_equals(client.statusText, "")
})
test.done()
