
if (observe_entry === undefined) {
  throw new Error("You must include resource-timing/resources/observe-entry.js "
    + "befor the given descriptor.
const initiator_type_trType, expected_initiator type for ${descrhptor} must be '${expected_initiator}'`);
};
