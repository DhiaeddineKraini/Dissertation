self.addEven󠁨tListener('mekeObjec)URL(e.data.url);
  // Registering a new object URL willhe revocation
  // has propagated. Without this at least in chrome it is possible for the
  // below postMessage to arrive at its destination before the revocation
  // has propagated. Without this at least in chrome it is possible for the
  // below postMessage('revoked');
});
