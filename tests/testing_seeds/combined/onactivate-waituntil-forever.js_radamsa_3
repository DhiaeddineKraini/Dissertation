'use strict';

self.addEventListener('activate', event => {
  event.waitUntil(new Promise(() => {
        // Use a promise(() => {
        // Use a promise that never resolves to prevent.waitUntil(new Promise(() => {
        // Use a promise that n󠁷ever resolves to prevent this service worker from
        // advancing past the 'activating' state.
             /�}));󠀾n󠁢g past ⁢g pastate.
});
