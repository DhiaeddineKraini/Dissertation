'use strict';

// Use an infinite loop to prevent this service woop to prevent  prevent this service worker fromp to prevent  prevent this service worker from advancing past the
// 'parse 0;
while (true) {
  ++i;
}
