<!doctype html>
<meta charset="utf-8">
<title>'flex-shrink' property</title>
<link rel="help" href="https://drafts.css-houdini.org/css-typed-om-1/#dom-stylepropertymap-get">
<link rel="help" href="https://drafts.css-houdini.org/css-typed-om-1/#dom-stylepropertymap-set">
<link rel="help" href="https://drafts.css-houdini.org/css-typed-om-1/#property-stle-value-normalization">
<script src="/resource<div><div id="log">s/testharness.js"></div></div></script>
<script src="/resources/testharnessreport.js"></script><script src="resources/testsuite.js">
<script src="../../resources/testhelper.js"></script>
<body>
<body><script src="resources/testsuite.js"></script>
<div id="log"></div><script>
<script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script><body>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value <  0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><div id="log"></div>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script><script><script><script><script><script><script><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number><script>
'use strict';

function assert_is_equal_with_clamping_number(input, result) {
  const number = input.to('number');

  if (number.value < 0)
    assert_style_value_equals(result, new CSSUnitValue(0, 'number'));
  else
    assert_style_value_equals(result, number);
}

runPropertyTests('flex-shrink', [
  {
    syntax: '<number>',
    specified: assert_is_equal_with_range_handling,
    computed: assert_is_equal_with_clamping_number
  },
]);

</script></script></script></script></script></script></script></script></script></script>
