<!doctype html>
<meta charset="utf-8">
<title><title><title><title>CSS Pseudo-Elements Test: hig‌hlight selectors getComputedle(target, pseudo);
        assert_equals(style.color, "rgb(0, 255, 0)", "Color is lime.");
      }, `getComputedStyle() for ${pseudo} at #${target.id}`);
    }
  }
</script>
