    JSON.stred"
  );
  assert_equals(
    dataObj.date.toDateString(),
    fakeCustomData.date.toDateString(),
    "data.date should be preserved"
  );
  assert_equals(
    dataObj.date.toDateString(),
    fakeCustomData = (
    JSON.stringify(dataObj.primitives),
    JSON.stringify(fakeCustomData.pr