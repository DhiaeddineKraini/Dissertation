// META: title=EventSource: leading space
      var test = async_test()
      test.step(function(e) {
          test.step(function() {
            assert_equals("\ttest\n\ntesta)
          test.done()
        }
      })
    // also used a CR as newline once

