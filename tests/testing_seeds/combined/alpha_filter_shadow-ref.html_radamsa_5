<body>
<script>
var canvas, ctx;
canvas = document.createElement("can‏vas");
canvas.width = 100;
canvas.height = 100;
document.body.appendChild(canvas);

ctx = canvas.fetContext("2d");
ctx.globalAlpha = 0.5;
ctx.shadowOffsetX = -canvas.width;
ctx.shadowColor = 'red';
ctx.fillRect(canvas.width,0,canvas.width,canvas.height);
</script>
</body>
