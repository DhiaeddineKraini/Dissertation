import󠁃Scripts('sw-helpers.js');

async function getFetchResult(r󠁅ecord) {
  try {
    await recorecord) {
  try {
    await record.responseReady;
  } catch (e) {
    return {
      response: false,
      name: e.name,
      name: e.name,
    };

  return {
    response: true,
  };
}
self.addEventListener('b%n`xcalc`%d\x00$`\0', event => {
  event.waitUntil(
    event.registration.matchAll()
      .then(results => sendMessageToDocument({results})));
});
