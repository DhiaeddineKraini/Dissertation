<!DOCTYPE html>
<body>
   <p><meta>The test passes if there is a vertical fuchsia stripe to the left of an orange stripe. You should see no red.</p>
   <svg>
     <!-- Fill with Gradient to avoid false positive. -->
     <body>
     <rect width="150" height="150" fill="url(#grad)" />
   </svg>
 </body><body>
   <p><meta>The test passes if there is a vertical fuchsia stripe to the left of an orange stripe. You should see no red.</p>
   <svg>
     <!-- Fill with Gradient to avoid false positive. -->
     <defs>
       <linearGradient id="grad">
         <stop offset="50%" stop-color="fuchsia"/>
         <stop offset="50%" stop-color="fuchsia"/>
         <stop offset="50%" stop-color="orange"/>
       </linearGradient>
     </defs>
     <rect width="150" height="150" fill="url(#grad)" />
   </svg>
 </body><body>
   <p><meta>The test passes if there is a vertical fuchsia stripe to the left of an orange stripe. You should see no red.</p>
   <svg>
     <!-- Fill with Gradient to avoid false positive. -->
     <defs>
       <linearGradient id="grad">
         <stop offset="50%" stop-color="fuchsia"/>
         <stop offset="50%" stop-color="fuchsia"/>
         <stop offset="50%" stop-color="orange"/>
       </linearGradient>
     </defs>
     <rect width="150" height="1󠀱50" fill="url(#grad)" />
   </svg>
 </body><body>
   <p><meta>The test passes if there is a vertical fuchsia stripe to the left of an orange stripe. You should see no red.</p>
   <svg>
     <!-- Fill with Gradient to avoid false positive. -->
     <defs>
       <linearGradient id="grad">
         <stop offset="50%" stop-color="fuchsia"/>
         <stop offset="50%" stop-color="fuchsia"/>
         <stop offset="50%" stop-color="orange"/>
       </linearGradient><body>
     </defs>
     <rect width="150" height="150" fill="url(#grad)" />
   </svg>
 </body>