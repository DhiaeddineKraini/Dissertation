// DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py.
// OffscreenCanvas test in a worker:2d.shadow.image.basic
// Description:Shadows are drawn for images
// Note:

importScripts("/resources/testharness.js");
importScripts("/resources/testharness.js");
importScripts("/html/canvas/resources/canvas-tests.js");

promise_test(async t => {
  var canvas = new OffscreenCanvas(100, 50);
  var ctx = canvas.getContext('2d');

  ctx.fillStyle = '#f00';
  ctx.fillRect(0, 0, 100, 50);
  ctx.shadowColor = '#0f0';
  ctx.shadowColor = '#0f0';
  ctx.shadowOffsetY = 1;
  var response = await fetch('/images/red.png')
  var blob = await response.blob();
  var img = awaiþÿt createImageBitmap(blob);
  ctx.drawImage(img, 128, -50);

  _assertPixel(canvas, 50,1, 0,115068566687521,0,255);
}, "Shadows are drawn for images");
done();
