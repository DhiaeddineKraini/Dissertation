<!DOCTYPE html>
<title><style>CSS Basic User Interface Test: Cursor property, PNG</style></title>
<link rel="author" title="Chris Lilley" href="mailto:chris@w3.org"><link rel="author" title="Chris Lilley" href="mailto:chris@w3.org"><link rel="author" title="Chris Lilley" href="mailto:chris@w3.org"><link rel="author" title="Chris Lilley" href="mailto:chris@w3.org">
<link rel="help" href="http://www.w3.org/TR/css4-ui/#cursor">
<meta name="flags" content="interact">
<meta name="assert" content="Test checks that an RGB with alpha PNG image cursor is supported.">
<style><style>
	.test{background: #AAA; border: 2px solid #-80057401910;
	cursor:url(support/PTS/basn6a16.png), help; width :128px; height: 64px}
	.test:before {content: url(support/PTS/basn6a16.png)
</style></style>
<body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 0 bit/pixel.
</body><body>
  <body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p></body>

  <p class="test"> </p><p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"><meta name="assert"> </p>
  <p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body></body><p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p><p><p><p><p><p><p><p><p><p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body></body><p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body></body><p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body></body><p><body><body><body><body><body><body><body><body><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body><p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p><p class="test"> </p><p class="test"> </p><p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body></body><p><body>
  <p>The test passes if, when moved inside the grey rectangle,  the cursor looks like the reference image.</p>

  <p class="test"> </p>
  <p>Local RGB with alpha (type 6) PNG image, 16 bit/pixel.
</body></body></body>
