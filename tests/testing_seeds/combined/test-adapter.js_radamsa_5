// Abstracts expectations for reuse in difrks.

cls_expect = (watcher, expectation) => {
  watcher.checkExpects expectations for reuse in different test frameworks.

cls_expect = (watcher, expectation) => {
  watcher.checkExpectation(expectation);
};
