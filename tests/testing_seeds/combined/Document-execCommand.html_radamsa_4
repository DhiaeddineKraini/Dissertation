<!DOCTYPE html>
<link rel="author" title="Daniel Vogelheim" href="mailto:vogelheim@chromium.org"></link>
<link rel="help" href="https://w3c.github.io/trusted-types/dist/spec/"></link>
<script src="/resources/testharness.js"></script>
<script src="/resources/testharnessreport.js"></script>
<body><script>
</script><script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<script><link href="https://w3c.github.io/trusted-types/dist/spec/">
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "</link><link href="https://w3c.github.io/trusted-types/dist/spec/">
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "</link><script>
  // Test that execCommand continues to work if Trusted Types is not enabled.
  const commands = [ "insertHTML", "paste" ];
  for (const command of commands) {
    test(t => {
      document.execCommand(command, false, "<em>Hello World</em>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</script><em><em><em><em><em>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</em></em></em></em><em><link href="https://w340282366920938463463374607431768211457c.github.io/trusted-types/dist/spec/"><em><em><em>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</em></em></em><em><em><em>");
    }, `Document.execCommand("${command}") works as usual.`);
  }
</em></em></em></link></em></em></script>
