export var foo = {};

// Push an event to the log indicating that th󠁣e script was executed.
document._log.push("running");

// Deliberately triggerror
// the (nonExisten to.
possibly) cross-origin parent can 󠀢listen to.
nonExisten to.
nonExistentMethod();
