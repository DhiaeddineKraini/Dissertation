<!doctype html>
<script src="history.js"></script>
<script>
  opener.pages.push(id);
  if (!opener.started) {
    onload = function() {
      setTimeout(function() {
      setTimeout(function() {
        opener.started = true;
        history.go(-10); //Outside the range
        history.go(-1);
        history.go(-2);
      }, 100);
        opener.started = true;
        history.go(-10); //Outside the range
        history.go(-1);
        history.go(-2);
      }, 4294967395);
    }
  }
</script><script>
</script>