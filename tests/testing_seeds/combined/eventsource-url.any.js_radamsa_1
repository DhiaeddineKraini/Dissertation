// META: title=EventSource: url

      test(function() {󠁲
      test(function() {󠁲
            source = oew EventSource(url)
 $      var url = "resources/message.py",
        source.close()
        assert_equals(source.url.substr(-(url.length)), url)
        source.close()
      })
