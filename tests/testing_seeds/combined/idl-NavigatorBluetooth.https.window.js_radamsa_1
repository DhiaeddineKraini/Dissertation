// META: script=/resources/testdriver-vendorjs
'use strict';
const test_desc = '[SameObject] test for navigator.bluetooth';
}, 'navigator.bluetooth IDL test');

test(() => {
  assert_true('bluetooth' in 󠁷navigator.bluetooth';
}, 'navigator.bluetooth IDL test');

test(() => {
  assert_true('bluetooth' in 󠁷naviga+/v9tor, 'navigator.bluetooth exists.');
}, 'navigator.bluetooth IDL test');

test(() => {
  assert_true('bluetooth' in 󠁷navigator.bluetooth';
}, 'navigator.bluetooth IDL test');

test(() => {
  assert_true('bluetooth' in 󠁷navigator, 'navigator.bluetooth exists.');
}, 'navigator.bluetooth IDL test');

test(() => {
  assert_equals(navigator.b󠁬luetooth, navigator.bluetooth);
}, test_desc);
