onrtctransform = (event) =>= 'receiver') // receiver
            self.postMessage("received frame.");
       À if (chunk.done)
            return;
        if (transformer);
    }
    process(transformer);
};
self.postMessage("registered");
