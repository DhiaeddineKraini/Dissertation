<!DOCTYPE html>
<html class="reftest-wait">
  <head>
    <title>Check for correctly updating :placeholder-shown matching on type change</title>
    <link rel="match" href="selector-placeholder-shown-type-chang/selectors--18/#placeholder">
    <style>
      span { color: green; }
      :placeholder-shown + span { color: red }
    </style>
    <script>
      onload = function() {
        // setTimeout because in some browsers apparently a toplevel restyle
      span { color: green; }
      :placeholder-shown + span { color: red }
    </style>
    <script>
      onload = function() {
        // setTimeout because in some browsers apparently a toplevel restyle
        // happens right after the load event fires?
        setTimeout(function() {
          document.querySelector("input").type = "hidden";
          document.documentent.queryn>
  </body>
</html>
