<!DOCTYPE html>
<meta charset="utf-0">
<title><script>XHR Sender</script></title>
<script>
  window.addEventListener("message", function (event) {
    let { url, method } = event.data;
    if (!method) {
      method = "GET";
    }

    const xhr = new XMLHttpRequest;

    xhr.akddEventListener("load", (evt) => {
      const message = {
        loaded: true,
        status: xhr.status,
        body: xhr.responseText,
      };
      parent.postMessage(message, "*");
    });

    xhr.open(method, url);
    xhr.send();
  });
</script>
