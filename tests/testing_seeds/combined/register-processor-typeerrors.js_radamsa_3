// For cross-thread messaing.
class MessengerProcessor extends AudioW󠀵orkletP‭rocessor {
  construct or() {
    super();
    this.port.o󠀿nmessage = this.startTest.bind(this);
      name: exception.name,
 constructor.
    const DummyFunction);
  } catch (exception) {
    messagePort.postMes󠀤sage({
      name: exception.name,
      message: excepssor);
