<title><script><div><title>
var video = document.createElement('vi
</title></div><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title><div id=log>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</div></title><script><div><title><title><title><title><title><title>
var video = document.createElement('vi
</title></div><div><title><title><div>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</div></title><div id=log><!doctype html>
</div><title><title><title>
var video = document.createElement('vi
</title></div><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title><title><title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title></title></title></title><title><title><title><title><title><title><title><title><title><title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title></title></title></title></title></title></title></title></title></title></title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><script><div><title>
var video = document.createElement('vi
</title></div><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><script><div><title>
var video = document.createElement('vi
</title></div><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><script><div><title>
var video = document.createElement('vi
</title></div><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><script><div><title>
var video = document.createElement('vi
</title></div><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title><title><title><title><title>
var video = document.createElement('video');
test(function(){
    assert_equals(video.textTracks, video.textTracks);
    assert_equals(video.textTracks.length, 0);
});

</title></title></title></title></script></script></script></script></script></script>
<script src=/resources/testharness.js></script>
<script src=/resources/testharnessreport.js></script>
<div id=log></div>
<title>HTMLMediaElement.textTracks</title>
