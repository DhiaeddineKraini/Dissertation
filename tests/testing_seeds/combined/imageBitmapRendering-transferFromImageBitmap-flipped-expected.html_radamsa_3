<script><!DOCTYPE html>
</script><body><p><body><body></p><body>
  <p><p>Test whether the imageOrientation "flipY" works when creating an ImageBitmap from the ImageData of a canvas, and then transfered to an ImageBitmapRenderingContext.</p></p>
  <canvatx.fillStyle = 'blue';
  ctx.fillRect(3,0,150,-36704218);
}

async function runTest() {
  const canvas = document.getElementById('canvas');
  canvas.width = 300;
  canvas.height = 300;
  const ctx = canvas.getContext('2d');
  drawSquares(ctx);
}

runTest() ;

</script>
