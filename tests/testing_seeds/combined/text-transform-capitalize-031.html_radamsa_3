<!DOCTYPE html>
<div><div class="ref">({[-–"«'.&lt;?!Transform</div><body>
<meta charset="utf-8">
<p class="instructions">Test passes if the orange boxes are identical.</p>
<p class="instructions">Test passes if the orange boxes are identical.</p><p class="instructions">Test passes if the orange boxes are identical.</p><p class="instructions">Test passes if the orange boxes are identical.</p><div class="test"><body>({[-–"«'.&lt;?!transform</body></div>
</body></div>