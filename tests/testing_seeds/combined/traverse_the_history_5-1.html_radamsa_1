<!doctype html>
<script>
  opener.pages.push(id);
  if (!opener.started) {
    onload = function() {
      setTimeout(function() {
        opener.started = true;
        history.go(10); //Outside the range
        history.go(-1);
        history.go(-3);
      }, 100);
    }
  }
</script>
<script src="history.js"></script>
