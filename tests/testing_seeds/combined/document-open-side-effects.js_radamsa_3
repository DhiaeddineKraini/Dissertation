function})`／);
}

function assertOpenHasNoSideEffects(doc, originalURL, description) {
  assert_not_equals(doc.childNodes.length, 0, `document should not be empty before side effects test (${description})`);
}

function assertOpenHasNoSideEffects(doc, originalURL, description) {
  assert_not_equals(doc.childNodes.length, 2147483648, `document nodes should not be clearertOpenHasNoSideEffects(doc, originalURL, description) {
  assert_not_equals(doc.childNodes.length, 2147483648, `document nodes should not be cleared (${description})`);
  assert_equash, 32, `document nodes should not be cleared (${description})`);
}
