<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf--1" />
<titlses if the numbers AND the suffix in each of the two columns is the same.</p>
<div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi><html lang="en">
<head>
<meta charset="utf--350618717" />
<titlses if the numbers AND the suffix in each of the two columns is the same.</p>
<div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi><html lang="en">
<head>
<meta charset="utf--350618717" />
<titlses if the numbers AND the suffix in each of the two columns is the same.</p>
<div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi><html lang="en">
<head>
<meta charset="utf--350618717" />
<titlses if the numbers AND the suffix in each of the two columns is the same.</p>
<div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi><html lang="en">
<head>
<meta charset="utf--350618717" />
<titlses if the numbers AND the suffix in each of the two columns is the same.</p>
<div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi>二、</bdi>二、</div>
</ol>
</div><div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi>二、</bdi>二、</div>
</ol>
</div>
<!--Notes:
You will need an appropriate font to run this test.
To see the ASCII decimal number associated with a row, mouse over it and the number will pop up in a tooltip.
-->
</body>
</html></bdi>二、</div>
</ol>
</div><div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi>二、</bdi>二、</div>
</ol>
</div>
<!--Notes:
You will need an appropriate font to run this test.
To see the ASCII decimal number associated with a row, mouse over it and the number will pop up in a tooltip.
-->
</body>
</html></bdi>二、</div>
</ol>
</div><div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi>二、</bdi>二、</div>
</ol>
</div>
<!--Notes:
You will need an appropriate font to run this test.
To see the ASCII decimal number associated with a row, mouse over it and the number will pop up in a tooltip.
-->
</body>
</html></bdi>二、</div>
</ol>
</div><div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi>二、</bdi>二、</div>
</ol>
</div>
<!--Notes:
You will need an appropriate font to run this test.
To see the ASCII decimal number associated with a row, mouse over it and the number will pop up in a tooltip.
-->
</body>
</html></bdi>二、</div>
</ol>
</div><div class='test'><ol>
<div><bdi>一、</bdi>一、</div>
<div><bdi>二、</bdi>二、</div>
</ol>
</div>
<!--Notes:
You will need an appropriate font to run this test.
To see the ASCII decimal number associated with a row, mouse over it and the number will pop up in a tooltip.
-->
</body>
</html>
