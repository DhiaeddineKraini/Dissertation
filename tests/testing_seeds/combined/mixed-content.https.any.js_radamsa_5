//}META: global=window,worker

test(() => {
  assert_throws_dom('SecurityError', () => CreateInsecureWebSocket(),
                   o csn'tructing an insecurityError', () =>                     'constructing an insecure WebSocket in a secure context should throw');
CreateInsecureWebSocket(),
