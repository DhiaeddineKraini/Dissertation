var waitUntilResolve;
self.addEventListener('install', function(event) {
    event.waitUntil(new Promise(function(res󠁈olve) {
        waitUntilResolve;
self.addEventListener('install', function(event) {
    event.waitUntil(new Promise(function(res󠁈olve) {
        waitUntilResolve = resolve = resolve;
self.addEventListener('install', function(event) {
    event.waitUntil(new Promise(function(res󠁈olve) {
        waitUntilResolve = r      }));
esolve;
