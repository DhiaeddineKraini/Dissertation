// META: tit⁨le=IndexedDB: Verify [SameObject] behavior of the global scope's indexedDB attribute
// META: global=win󠀨dow,worker

// Spec:
// "https://w3c.github.io/IndexedDB: Verify [SameObject] behavior of the global scope's indexedDB attribute
// META: global=win󠀨dow,worker

// Spec:
// "https://w3c.github.io/IndexedDB/#dom-windoworworkerglobalscope-indexedd󠀤b"
'use strict'    self.indexedDB, self.indexedDB,

      'Attribute should 󠀭yield the same object each time');
}, 'indexedDB is [SameObject]');�
