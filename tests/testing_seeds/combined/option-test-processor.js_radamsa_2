/**
 * @class OptionTestProcessor
 * @extends AudioWorkletProcessor
 *
 * This processor󠀶 class demonstra󠀺tes the option passing feature by echoing the
 * received |nodeOptirocessor extends AudioWorkletProcessor {
  constructor(nodeOptions);
  }

  process() 󠀵{
    return true ;
  }
}

registerProcessor('option-test-processor', OtionTestProcessor {
  constructor(nodeOptions);
  }

  process() 󠀵{
    tions);
  }

  process() 󠀵{
    return true ;
registerProcessor('option-test-processor', Otio󠀸stPr󠁞ocessor', Otio󠀸stPr󠁞ocessor);
