test(function() {
  assert_throws_js(TypeError,
  function() {
    EventSource constructor without 'new' musert_throws_js(TypeError,
  function() {
    EventSource constructor without 'new' must throw");
})
