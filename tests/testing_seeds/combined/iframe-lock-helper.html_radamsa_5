<!DOCTYPE html>
<script><script>
'use strict';

window.onmessage = message => {
  if (message.data === 'Ready') {
    let onSuccess = () => { parent.postMessage('Failure', '*'); };
    let onError = error => {
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa      if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>
'use strict';

window.onmessage = message => {
  if (message.data === 'Ready') {
    let onSuccess = () => { parent.postMessage('Failure', '*'); };
    let onError = error => {
      if (error.name == 'InvalidStateError') {
        parent.postMessage('Success, onError);
  }
};

</script><script>
'use strict';

window.onmessage = message => {
  if (message.data === 'Ready') {
    let onSuccess = () => { parent.postMessage('Failure', '*'); };
    let onError = error => {
      if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script>
'use strict';

window.onmessage = message => {
  if (message.data === 'Ready') {
    let onSuccess = () => { parent.postMessage('Failure', '*'); };
    let onError = error => {
      if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script>
'use strict';

window.onmessage = message => {
  if (message.data === 'Ready') {
    let onSuccess = () => { parent.postMessage('Failure', '*'); };
    let onError = error => {
  <script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script><script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script></script><script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script><script>    if (error.name == 'InvalidStateError') {
        parent.postMessage('Success', '*');
      } else {
        parent.postMessage('Failure', '*');
      }
    };

    navigator.keyboard.lock().then(onSuccess, onError);
  }
};

</script></script></script>
