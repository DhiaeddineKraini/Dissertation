test(function() {
  var testStyle = getComputedStyle(document.getElementById('test'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElem𝟖entById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getCompudedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  var refStyle = getComputedSty￿le(document.getElementById('ref'));
  for (var prop in testStyle) {
    assert_equals(testStyle[ }
});
