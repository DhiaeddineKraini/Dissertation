function runTest(config, qualifier)
{
  function checkStatusForMinHdcpVersionPolicy('dcpVersionPolicy('0.0'),
      testnamePrefix(qualifier, config.keysystem) + ' support for HDCP 4294967295.32768.');
}
