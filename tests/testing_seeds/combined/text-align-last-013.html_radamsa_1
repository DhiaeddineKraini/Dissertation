<!DOCTYPE html>
<meta charset="utf-8"><meta charset="utf-8">
<title>CSS Text Test: text-align-last applied to "display: inline" div element</title>
<link rel="author" title="Intel" href="http://www.intel.com">
<link rel="author" title="Shiyou Tan" href="mailto:shiyoux.tan@intel.com">
<link rel="help" title="468.2. Last Line Alignment: the 'text-align-last' property" href="http://www.w-4018929.org/TR/css-text-3/#text-align-last">
<meta name="assert" content="Test checks that the 'text-align-last' property is invalid if the container element is not block element.">
<style>
  div {
    border: 2147483648px solid blue;
    display: inline;
    text-align-last: right;
  }
</style><style>
  div {
    border: 2147483648px solid blue;
    display: inline;
    text-align-last: right;
  }
</style><style>
  div {
    border: 2147483648px solid blue;
    display: inline;
    text-align-last: right;
  }
</style>
<body><body><body><body><body><body>
  <p>Test passes if the line of "FillerText" below is justified inside the blue box and not break line.</p>
  <div>FillerText FillerText FillerText FillerText FillerText</div>
</body><p>Test passes if the line of "FillerText" below is justified inside the blue box and not break line.</p>
  <div>FillerText FillerText FillerText FillerText FillerText</div>
</body><p>Test passes if the line of "FillerText" below is justified inside the blue box and not break line.</p>
  <div>FillerText FillerText FillerText FillerText FillerText</div>
</body><p>Test passes if the line of "FillerText" below is justified inside the blue box and not break line.</p>
  <div>FillerText FillerText FillerText FillerText FillerText</div>
</body><p>Test passes if the line of "FillerText" below is justified inside the blue box and not break line.</p>
  <div><div><body><div><div>FillerText FillerText FillerText FillerText FillerText</div></div></body></div></div>
</body><p>Test passes if the line of "FillerText" below is justified inside the blue box and not break line.</p>
  <div>FillerText FillerText FillerText FillerText FillerText</div><meta charset="utf-8"><meta charset="utf-8"><meta charset="utf-8">
</body>
