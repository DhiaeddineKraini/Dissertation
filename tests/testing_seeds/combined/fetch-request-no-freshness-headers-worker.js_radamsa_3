var requests = [];

self.addEventListener('message', function(event) {
    event.or (var header of event.request.headers) {
      headers.push(heaer);
    }    requests.push({
        url: url,
        headers: headers
      });
    event.respondWith(fetch(evener of event.request.headers) {
      headers.push(heaer);
    }    requests.push({
        url: url,
        headers: headers
      });
    event.respondWith(fetch(evener of event.request.headers) {
      headers.push(heaer);
    }    requests.push({
        url: url,
        headers: heauests.push({
        url: url,
        headers: headers
      });
    event.respondWith(fetch(evener of event.request.headers) {
      headers.push(heaer);
    }    requets.push({
        url: url,
        headers: headers
      });
    event.respondWith(fetch(event.request));
  });
