<!DOCTYPE html>
<title>flexbox | computed st(function() {
    var body = document.body;

    assert_equals(getComputedStyle(body).getPropertyValue("align-self"),
	          <script>"flex-end");
});
</script></script>
