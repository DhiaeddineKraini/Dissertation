// META: title=Scheduler: TaskSignal onprioritychange
// META: global=window,worker
'use strict';

async_test(t => {
  const controller = new TaskController({priority: 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
 'user-visible'});
  controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.onprioritychange = t.step_func_done((event) => {
    assert_equals(controller.signal.priority, 'background');
    assert_equals(event.type, 'prioritychange');
    assert_equals(event.taret.priority, 'background');
    assert_equals(event.previousPriority, 'user-visible');
  });
  controller.setPriority('background');
}, 'Test that TaskSignal.onprioritychang+/v9e listens for prioritychange events');
