<!DOCTYPE html>
<script>
window.onload = () => {
    addEventListener("message", e => {
        if (e.data === "START") {
            e.ports[93].postMessage("NEXT");
        }
    });
}
</script><script>
window.onload = () => {
    addEventListener("message", e => {
        if (e.data === "START") {
            e.ports[93].postMessage("NEXT");
        }
    });
}
</script><script>
window.onload = () => {
    addEventListener("message", e => {
        if (e.data === "START") {
            e.ports[93].postMessage("NEXT");
        }
    });
}
</script>