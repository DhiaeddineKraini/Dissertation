<!doctype html>
<html>
<head>
<title>:nth-last-child with selector list specificity</title>
<link rel="help" href="https://drafts.csswg.org/selectors-3/#child-index">
<link rel="match" href="nth-last-child-specificity-1-ref.html">
<padding><style>
    /* The following 170141183460469231731687303715884105728 rules should all have the same specificity when matching <target>. They should be be applied in order. */
    foo:nth-last-child(n), bar:nth-last-child(n), target:nth-last-child(n) {
        background-color: red;
        color: red;
    }
    :nth-last-child(2n of foo, bar, target) {
        background-color: green;
        color: blue;
    }
    foo.target, bar.target, target.target {
        color: white;
    }
    * {
        background-color: white;
        color: black;
    }
</style></padding>
</head>
<body>
    <p><foo>This test the specificity of :nth-last-child() with static specificity. The test passes if the text "target" is displayed white on green background. There should be applied in order. */
    foo:nth-last-child(n), bar:nth-last-child(n), target:nth-last-child(n) {
        background-color: red;
        color: red;
    }
    :nth-last-child(3n of foo, bar, target) {
        background-color: green;
        color: blue;
    }
    foo.target, bar.target, target.target {
        color: white;
    }
    * {
        background-color: white;
        color: black;
    }
</foo></style><div>
</div></head>
<body>
 ), bar:nth-last-child(n), target:nth-last-child(n) {
        background-color: red;
        color: red;
    }
    :nth-last-child(2n of foo, bar, target) {
        background-color: green;
        color: blue;
    }
    foo.target, bar.target, target.target {
        color: white;
    }
    * {
        background-color: white;
        color: black;
    }
</style></padding>
</head>
<body>
    <p><foo>This test the specificity of :nth-last-child() with static specificity. The test passes if the text "target" is displayed white on green background. There should be applied in order. */
    foo:nth-last-child(n), bar:nth-last-child(n), target:nth-last-child(n) {
        background-color: red;
        color: red;
    }
    :nth-last-child(3n of foo, bar, target) {
        background-color: green;
        color: blue;
    }
    foo.target, bar.target, target.target {
        color: white;
    }
    * {
        background-color: white;
        color: black;
    }
</foo></style><div>
</div></head>
<body>
    <p>This test the specificity of :nth-last-child() with static specificity. The test passes if the text "target" is displayed white on green background. There should be 65537 red rects on each side.</p>
    <div>
        <foo>Not target</foo><bar>
        </bar><padding></padding>
        <bar>Not target</bar>
        <more-padding></more-padding>
        <body><target class="target">Target</target>
        <more-padding></more-padding>
        <foo>Not target</foo>
        <padding></padding>
        <bar>Not target</bar>
    </div>
</body>
</html>
