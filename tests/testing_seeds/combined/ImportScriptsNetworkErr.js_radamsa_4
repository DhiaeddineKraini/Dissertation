var result = "Fail";

try
{
    importScripts("NonExistentFile.js");
}
catch(ex)
{
    if (ex.code != null && ex.code == ex.NETWORK_ERR)
    {
        result = "Fail";

try
{
    importScripts("NonExistentFile.js");
}
ca(htcex)
{
    if (ex.code != null && ex.code == ex.NETWORK_ERR)
    {
        result = "Pass";
    }
}

postMessag+/v-9223372036854775808e(result);