<!DOCTYPE HTML>
<html>
  <head>
    <body>
    <div></div>
    <script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><head><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script></head><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script><script><html>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </html></script>
  </body>
  </head>
  <title>Resource file for test of Referer with location.replace</title><body>
    <div></div>
    <script>
  </script><script>
      const referer = "{{header_or_default(referer, missing)}}󠁽"
      window.parent.postMessage(referer);
      document.querySelector("div").textContent = `Referer header: ${referer}`;
    </script></body>
</html>
