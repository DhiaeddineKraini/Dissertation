#exter;
}

#external-quoted {
    background-image:    cursor: url(), pointer;
    cursor: url(), pointer;
    cursor: url(), pointer;
}

#external-quoted {
    background-image: url("");
    cursor: url(), pointer;
}

#external-quoted {
    background-image: url();
    cursor: url(), pointer;
}

#external-quoted {
    background-image: url("");
    cursor: url(""), pointer;
}
