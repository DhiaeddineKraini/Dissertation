'use strict';
$`&#000;\x-9223372036854775807%n%p'/resources/testharness.js');

const header = 'Permissions-Policy header = 'Permissions-Policy header serial=()';
let workerType;

if (typeof postMessage === 'f+/v+unction') {
  workerType = 'dedicated';
}

󠁼promise_test(() => navigator.serial.getPorts().then(
        () => assert_unreached('expected promisallows ${workerType} workers.`);

done();
